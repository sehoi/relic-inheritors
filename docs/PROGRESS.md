# 진행 저널

자율 루프가 이터레이션마다 **최상단에** 한 항목씩 추가한다.
위에서부터 읽으면 최근 진행 상황이 파악된다.

형식:

```md
### YYYY-MM-DD · T-0NN · <제목>
- 한 일:
- 알게 된 것:
- 다음에 걸릴 것 같은 것:
```

---

### 2026-08-30 · 터치 조작 · 데스크톱 전제였던 게임에 화면마다 가상 버튼을 얹었다
- 한 일: `game/ui/TouchControls.ts` 신규. `data/keys.ts`의 `SceneKeys`를 그대로 읽어 D-패드 + 원형 주 버튼 + 보조 버튼 줄을 그린다. 9개 씬 전부에 `getTouchControls().setKeys(...)` 한 줄씩 연결
- 알게 된 것:
  - **배포된 빌드가 모바일에서 검은 화면이 아니라 "탭할 게 없는" 화면이었다.** 실사용자가 진단용 아티팩트에서 타이틀은 뜨는데 `PRESS ENTER`를 누를 키보드가 없다고 짚어줬다 — 이 프로젝트는 GDD·아키텍처 어디에도 터치 언급이 없이 처음부터 키보드 전제로 설계돼 있었다.
  - **`Image.src`를 가로채는 접근은 틀린 계층이었다.** 처음엔 가상 버튼이 `HTMLImageElement.prototype.src`를 흉내내려 했는데(에셋 임베드 실험에서 썼던 기법을 재활용하려다), Phaser의 `ImageFile`은 기본적으로 `XMLHttpRequest`로 먼저 받아 Blob으로 바꾼 뒤에야 `Image`에 얹는다 — 이건 애초에 버튼과 무관한 별개 실험(모바일 진단용 아티팩트)에서 배운 것이었다.
  - **버튼과 씬 코드 사이를 진짜 `KeyboardEvent`로 이었다.** Phaser의 `KeyboardPlugin`이 `event.key`/`.code`가 아니라 `event.keyCode`(레거시 숫자)만 보고 `keydown-X` 이벤트와 `Key.isDown`을 판정한다는 것을 소스에서 확인했다 — `KeyboardEvent` 생성자는 `keyCode`를 못 받게 막아놔서 `Object.defineProperty`로 강제로 얹어야 했다. 이 덕분에 **씬 쪽 입력 코드는 한 줄도 안 바꿨다** — 물리 키보드와 가상 버튼이 완전히 같은 경로를 탄다.
  - **배치 데이터가 하나의 출처라는 게 여기서도 값을 했다.** `KeyGuide`(도움말)와 같은 이유로 버튼도 `data/keys.ts`를 그대로 읽게 만들어서, 새 화면이나 새 단축키가 생겨도 터치 버튼을 따로 안 그려도 된다.
  - **Playwright `.tap()`은 순간적이라 이동 검증에 못 썼다.** 방향키는 눌려 있는 동안(`isDown`) 계속 걷는 구조라, `pointerdown` → 대기 → `pointerup`을 직접 `dispatchEvent`로 흉내내야 카메라가 실제로 움직이는 것까지 확인할 수 있었다.
- 다음에 걸릴 것 같은 것:
  - 이 확인은 로컬 Playwright 실험이었지, `tests/smoke/`에 편입된 자동 회귀가 아니다. 터치 뷰포트로 D-패드/버튼을 누르는 스모크가 없으면 다음에 씬을 늘릴 때 조용히 깨질 수 있다
  - 터치 버튼 레이아웃은 화면(타이틀·오버월드 등)마다 자동 생성이라 아직 사람 눈으로만 훑었다 — 전투처럼 요소가 빽빽한 화면에서 겹침이 있는지 스크린샷으로 한 번 더 봐야 한다

### 2026-08-30 · 배포 확인 · 프로덕션 빌드에서 에셋이 통째로 빠져 있었다
- 한 일: GitHub Pages 배포 가능 여부를 확인하다가 `vite build` 산출물에 `assets/`(타일셋·스프라이트시트 295개 파일)가 아예 없는 것을 발견. `scripts/copy-assets.mjs`로 빌드 후 복사, `.github/workflows/deploy-pages.yml` 추가
- 알게 된 것:
  - **스모크가 `npm run dev`(파일시스템 전체 서빙)를 대상으로 돌아서 이 문제를 못 잡았다.** `assets/`가 `public/`이 아닌 저장소 루트에 있어, dev 서버에서는 아무 파일이나 경로로 열리지만 `vite build`는 `public/`만 그대로 복사한다 — 정적 호스팅(GitHub Pages)에 올려야 처음 드러나는 종류의 버그다
  - **빌드 산출물로 실제 확인하지 않으면 "배포 가능"이라고 말할 수 없었다.** `vite preview`는 SPA 폴백으로 없는 파일도 200(`text/html`)을 돌려줘서 처음엔 안 걸렸고, 헤더의 `Content-Type`을 봐야 진짜 파일인지 폴백인지 구분됐다. 진짜 정적 서버(`python -m http.server`)로 GitHub Pages 서브패스(`/relic-inheritors/`)를 흉내내 다시 확인하고서야 잡혔다
  - `vite.config.ts`의 `base: './'`(상대 경로)는 이미 맞게 되어 있어 손대지 않았다 — 프로젝트 페이지든 커스텀 도메인이든 서브패스에 안전하다
- 다음에 걸릴 것 같은 것:
  - 배포를 마치려면 이 브랜치를 `main`에 머지하고, 저장소 Settings → Pages → Source를 "GitHub Actions"로 켜는 사람의 조작이 남아 있다 (관리자 권한이 필요해 이 세션에서는 못 함)
  - `npm run smoke`가 여전히 dev 서버만 본다. 프로덕션 빌드를 대상으로 한 스모크(또는 CI에 `vite preview` 단계)가 없으면 이런 종류의 회귀가 다시 조용히 들어올 수 있다 — 백로그에 적어둘 만하다

### 2026-08-06 · T-042·T-048·T-043 · 남은 UX 셋. 입력을 읽는 것만으로 소비된다
- 한 일: 전멸 시 선택 창, 여관 확인 창, 플레이 시간 정산. `game/ui/ChoiceBox` 신규. 테스트 765 → 776개, 스모크 19 → 21개
- 알게 된 것:
  - **셋 중 둘이 같은 기제였다.** "값을 치를까" 와 "여기서 다시 시작할까" 는 둘 다 갈림길이고, 대화 스크립트에 선택지를 넣는 대신 UI 를 나눴다 — **대화는 읽는 것이고 이건 고르는 것이다.** 스키마를 키웠으면 대사가 아닌 것을 대사 자리에 넣게 됐을 것이다.
  - ⚠️ **`JustDown` 은 부르는 순간 소비된다.** 닫힌 창에도 입력을 넘기려고 `handle({ confirm: this.interactJustPressed() })` 를 매 프레임 불렀더니, 그 아래 `if (this.interactJustPressed())` 가 언제나 false 였다 — 여관 앞에서 Space 를 눌러도 아무 일이 없었다. **"물어보고 버리는" 것과 "안 물어보는" 것이 다르다.** 창이 열려 있을 때만 읽도록 고쳤다.
  - **`stopClock` 은 만들어만 두고 아무 데서도 부르지 않고 있었다.** T-043 은 "시간이 새는지 확인" 이었는데, 실제 문제는 새는 게 아니라 **멈추지 않는 것**이었다 — 전멸하고 타이틀을 한참 들여다보는 동안에도 시간이 흘렀다. 죽은 코드가 곧 빠진 호출이었다.
  - **타이밍을 짚는 테스트를 쓰려다 그만뒀다.** 전멸 뒤 Enter 를 몇 번 누르면 어디까지 가는지는 한 프레임 차이로 달라져서, 그걸 맞히려 들면 테스트가 동작이 아니라 속도를 재게 된다. **어디에 닿는가**만 보도록 바꿨다.
- 다음에 걸릴 것 같은 것:
  - 상점은 아직 묻지 않고 산다. 값이 목록에 늘 떠 있어 여관만큼 급하지 않지만, 같은 `ChoiceBox` 로 붙일 수 있다
  - M5 의 남은 정의는 "40분 플레이 가능" 하나다. 3층 소모전 측정과 함께 통째로 재볼 시점이다

### 2026-08-06 · T-064 · 넘기지 않은 값이 지난 회차를 데려왔다
- 한 일: 새 게임 위치 초기화, 메뉴의 저장/이어하기 분리, 세이브 화면 닫기 경로 수정. 스모크 17 → 19개
- 알게 된 것:
  - **인자를 생략하면 Phaser 는 그 씬에 마지막으로 넘긴 데이터를 남겨둔다.** `scene.start('overworld')` 로 새 게임을 시작했더니 죽은 자리에서 다시 시작했다 — 파티는 새것인데 위치만 지난 회차의 것이었다. **"안 넘김" 과 "빈 값을 넘김" 이 다르다**는 것을 여기서 처음 겪었다.
  - **같은 실수가 메뉴에도 있었다.** `mode` 를 안 넘겨 "저장" 이 기본값 `load` 로 열렸다 — 저장하러 들어가서 불러오기 화면을 만나는데, 두 화면이 비슷하게 생겨 눈으로도 안 띈다. 하루 사이에 같은 모양의 버그가 두 곳에서 나왔다: **기본값이 있는 인자를 생략하면 호출부가 조용히 틀린다.**
  - **재현을 시도한 덕에 첫 테스트가 가짜였다는 것을 알았다.** `page.goto` 로 두 번 열어 확인하려 했는데 Phaser 인스턴스가 새로 생겨 씬 데이터가 남지 않았다 — 고치기 전 코드로도 통과했다. 고친 코드를 되돌려 보지 않았으면 초록을 믿었을 것이다.
  - **재현을 두 번 놓친 뒤 "못 한다" 고 결론지었는데 틀렸다.** `?at=` 으로 보스 앞에 서는 길만 보고 막혔다고 판단했지만, **계단으로 지하에 내려가면 플래그가 필요 없다** — 층 이동이 곧 "값과 함께 씬을 시작하는" 자리이고, 지하는 파티 Lv1 이 몇 판 안에 죽는다. 사용자가 "보스에서 전멸했는데 확인을 못 하냐" 고 짚어줘서 다시 봤다.
  - 배운 것: **"재현 못 한다" 는 결론은 경로를 다 훑고 나서 내려야 한다.** 한 경로가 막혔을 때 그것을 전체의 한계로 읽었고, 그 상태로 한계를 주석에 적어 굳힐 뻔했다. 고치기 전 코드에서 `ruin-depths (5,6)` 으로 되살아나는 것까지 확인하고서야 테스트가 진짜가 됐다.
- 다음에 걸릴 것 같은 것:
  - T-042(전멸 시 마지막 저장 지점 복귀)를 하면 이 경로 자체가 바뀐다. 그때 이 테스트도 다시 봐야 한다
  - 씬에 값을 넘기는 자리가 늘고 있다 (returnTo, mode, location, openHelp, dropRelicId). 생략 가능한 인자가 늘수록 같은 실수가 반복된다

### 2026-08-06 · T-054 · 거점 사람들. M5 가 끝났다
- 한 일: 거점 NPC 5명 + 대화, `core/world/reach.ts`(길막힘 검사). 테스트 753 → 765개
- 알게 된 것:
  - **대사에 수를 적으면 데이터가 늘 때 거짓말이 된다.** "열셋까지 세었어" 로 썼다가 지웠다 — 유물을 늘린 사람이 대사까지 고칠 이유는 없고, 그건 T-055 에서 조작 안내가 어긋나 있던 것과 같은 함정이다. 힌트의 값어치는 수가 아니라 **성질**에 있다: "아직 안 주워 온 게 있다" 와 "가방이 먼저 찬다" 는 수가 바뀌어도 참이다.
  - **거점 입구 줄에 사람을 세웠다가 스모크가 막혔다.** NPC 는 벽처럼 걸음을 막으므로, 포탈에서 직진하는 줄에 서면 문을 막는 꼴이 된다.
  - 그래서 만든 `reach` 검사가 **그 케이스는 못 잡는다.** 거점이 넓어 우회로가 있어 도달 자체는 되기 때문이다 — **불편한 것과 끊긴 것은 다르다.** 검사를 만들고 나서 "이게 방금 그 버그를 잡나?" 를 물어본 덕에 알았고, 주석에 그 한계를 적었다. 잡는 것은 좁은 복도와 계단이고 그건 실제로 위험한 쪽이다.
  - 검사에 이빨이 있는지는 **실제 데이터가 아니라 그림으로 그린 맵**에서 확인했다. `'#..#..#'` 같은 문자열로 복도를 만들면 무엇을 재는지가 코드에 그대로 보이고, 게임 데이터가 바뀌어도 이 검사의 뜻은 흔들리지 않는다.
- 다음에 걸릴 것 같은 것:
  - **M5 의 표가 다 찼다.** 남은 것은 미완 UX(T-042 전멸 복귀, T-048 확인 창, T-043 시간 정산)와 3층 소모전 측정이다
  - 콘텐츠가 다 들어왔으니 이제 "40분 플레이" 가 실제로 성립하는지 통째로 재볼 수 있다 (GDD §8 의 M5 정의)

### 2026-08-06 · T-053 · 아이템 20종. 살 수 없던 자원이 살 수 있게 됐다
- 한 일: 아이템 5 → 20종, `heal` 효과에 MP 축 추가, 상점 재고·창 넘김. 테스트 750 → 753개
- 알게 된 것:
  - **MP 는 살 수 없는 자원이었다.** 승리 보상(적 하나당 1)이 유일한 공급원이라, 마르면 되돌릴 수단이 전투 안에 없었다 — T-046 에서 가동률을 98%까지 올려놨지만 그건 "마르지 않게 하는" 쪽이었고 "마른 뒤" 는 여전히 답이 없었다.
  - **새 효과를 만드는 대신 등급으로 나눴다.** 효과가 늘면 전투 규칙이 늘지만 등급이 늘면 선택만 는다. 20종을 채우는 데 새 규칙은 하나(MP)면 충분했다.
  - **등급은 위로 갈수록 효율이 좋아야 한다.** 그러지 않으면 싼 것을 여러 개 사는 것이 언제나 정답이고 등급이 장식이 된다. 소지 한도(9)가 있어 "자리를 아끼는 값" 이 성립하는 것이 그 전제다.
  - **규칙이 콘텐츠 수에 매여 있었다.** "부활이 가장 비싸다" 는 아이템 5종일 때 쓴 것이고, 등급이 생기자 최상급 회복이 하급 부활보다 비싼 것이 자연스러워졌다 — 값이 틀린 게 아니라 규칙이 낡은 것이었다. **뜻을 유지한 채 모양을 다시 썼다.**
  - **상점 목록이 또 화면을 넘쳤다** (T-057 과 같은 종류). 이번에는 이미 만들어둔 `core/ui/scroll` 을 끼우는 것으로 끝났다 — 한 번 제대로 뺀 것이 두 번째부터 값을 한다.
- 다음에 걸릴 것 같은 것:
  - 시작 소지품은 그대로 5종이다. MP 아이템을 넣으면 소모전이 쉬워지므로 일부러 안 넣었다 — 상점에서 사는 것이 자연스럽고, 그게 은편의 쓸 곳이기도 하다
  - M5 에 남은 것은 거점 NPC 5명(T-054) 하나다

### 2026-08-06 · T-051 · 보스. 양면성이 곧 필수 유물이었다
- 한 일: 성소 보스(2페이즈, 유물 드랍), 유물 13종째, 측정 지점 재확인, 타일 뷰어. 테스트 749 → 750개, 스모크 16 → 17개
- 알게 된 것:
  - **보스가 유물을 드랍하려면 유물이 하나 더 필요했다.** 12종이 시작 8 + 회수 4 로 딱 맞물려 있어 뺄 자리가 없었다 — 콘텐츠 항목끼리 이미 서로를 붙들고 있다는 뜻이다.
  - **처음 만든 드랍 유물은 필수 유물이었다.** `atk 7 / mag 7` 로 물리와 마법 양쪽에 붙으니 어떤 편성에도 쓸모 있어, 빼고 짠 최고 조합이 28%p 손해였다(기준 25%p). 수치를 낮추는 대신 **한쪽으로 몰았다** — 양면성 자체가 문제였지 총량이 아니었다. T-035 의 `잿빛 등`(위력 170 액티브를 0단계에 둔 것)과 같은 모양의 실수다.
  - 유물이 13종이 되어도 **측정 지점(Lv37)은 그대로였다.** T-036 이 걱정하던 "통과 구간이 좁아진다" 가 이번에는 벌어지지 않았다 — 필수 유물을 고치자 구간이 오히려 돌아왔다. 구간이 좁아지는 원인이 유물 수만은 아니라는 뜻이다.
  - **처치 여부를 저장하지 않기로 했다.** 드랍 유물을 지녔는지가 곧 처치 여부이고 유물을 잃는 기제가 없으므로 둘은 같은 뜻이다. 상태를 둘로 두면 어긋날 자리가 생기는데, 여기서는 그 자리를 아예 만들지 않았다 — 세이브 버전을 올리지 않은 것이 부수 효과다.
  - **타일 번호를 세 번 틀렸다** (111, 126, 그리고 124 의 이름 불일치). 결국 시트를 번호와 함께 펼쳐 보는 임시 페이지를 만들어 한 번에 골랐다 — **눈으로 확인해야 하는 것은 눈으로 볼 수 있게 만드는 편이 빠르다.** 세 번 반복하고 나서야 그렇게 했다.
- 다음에 걸릴 것 같은 것:
  - M5 남은 것은 아이템 20종(T-053)과 거점 NPC 5명(T-054)뿐이다. 둘 다 새 기제 없이 표를 채우는 일이다
  - 3층 소모전은 여전히 재지 않았다. 보스는 쟀지만 거기 닿기까지의 소모는 다른 질문이다

### 2026-08-06 · T-052 · 3층과 잠긴 문. 아무것도 재지 않는 테스트를 쓸 뻔했다
- 한 일: `ruin-sanctum` 맵, 잠긴 문 기제, 열쇠 보상, 세이브 v6, `?at=` 개발 플래그. 테스트 736 → 749개, 스모크 15 → 16개
- 알게 된 것:
  - **막는 것이 목적이 아니라 순서를 만드는 것이 목적이다.** 열쇠를 같은 층에 두어 한 바퀴 돌게 했다 — 아래층에 두면 없는 채로 내려온 사람이 되돌아가야 하고, 그건 난이도가 아니라 왕복이다.
  - **문 앞에 설 방법이 없어서 아무것도 재지 않는 테스트를 썼다가 고쳤다.** 두 층을 가로질러 걸어가는 것이 부담스러워 "에러만 없으면 통과" 하는 껍데기를 만들었는데, 그건 초록이 아무 뜻도 없는 테스트다. `?at=맵:x,y` 로 그 자리에 설 수 있게 만든 뒤 **실제 경로 36칸을 걷는** 테스트로 바꿨다 — 열쇠 없이 막히고, 되돌아가 줍고, 다시 와서 열린다.
  - **먼 자리에서 벌어지는 일을 확인하려면 거기 설 수 있어야 한다.** `?party=full`(T-057) 과 `?mobs=all`(T-050) 에 이어 세 번째다. 셋 다 "테스트가 밟지 않는 상태" 를 열어주는 뒷문이고, 그런 상태가 곧 버그가 사는 곳이었다.
  - 맵을 손으로 짜다 두 번 틀렸다. **문이 전실과 겹치지 않는 높이에 있어 어느 쪽에서도 닿지 않았고**, 지하 계단을 탐구자가 서 있는 칸에 놓았다. 둘 다 기존 검사(구역 빈틈·포탈 위 NPC)가 잡았다 — 맵을 데이터로 검사해 두면 손으로 짠 좌표의 실수가 화면 전에 걸린다.
- 다음에 걸릴 것 같은 것:
  - 3층에 보스를 놓는 것이 T-051 이다. 성소가 안전지대라 그 안에서는 조우가 없다 — 보스는 조우가 아니라 **자리에 놓인 것**이어야 한다
  - 3층 소모전은 아직 재지 않았다. 적 레벨 10 은 1층(7)에서 셋 올린 값이고 근거는 층 간격뿐이다

### 2026-08-06 · T-050 · 잡몹 6종. AI 성격이 스탯보다 세게 먹힌다
- 한 일: 잡몹 2 → 6종, AI 프로필 4종 추가, 지역별 등장 표(`AREA_MOBS`), `?mobs=all`. 테스트 733 → 736개, 스모크 14 → 15개
- 알게 된 것:
  - **속성 약점만 다르면 적이 여섯이어도 싸움은 하나다.** 무엇을 하는 놈인지가 달라야 대상 선택과 조합이 달라진다. 속성 · 스탯 배율 · AI 를 각각 다른 축으로 갈랐고, 셋 중 AI 가 가장 크게 체감된다.
  - **AI 성격이 스탯보다 세게 먹혔다.** `wisp` 는 HP 0.7 짜리 약한 적인데, "약한 쪽만 노린다" 는 성격 하나로 시작 지역 전멸률을 0% → 5% 로 올렸다. 다른 적이 흩어서 주는 피해를 한 사람에게 모아주기 때문이고, **파티가 둘뿐인 구간에서 그건 곧 전멸이다.** 스탯을 만져 되돌렸지만, 다음에 AI 를 추가할 때도 같은 크기로 흔들릴 것이다.
  - **타일 번호는 눈으로 봐야 안다.** 시트에서 번호만 보고 골랐더니 "떠도는 불씨" 자리에 빨간 머리 사람 얼굴이 서 있었다 — 테스트는 전부 초록이었다. 종류가 여섯이면 무작위 조우로 전부 확인하는 데 몇 판이 걸릴지 알 수 없어서 `?mobs=all` 로 한 줄에 세워 놓고 본다.
  - **콘텐츠를 늘릴 때마다 "어디서도 안 나오는 것" 검사가 필요해진다.** 유물 둘이 그랬고(T-062) 잡몹도 같은 구조다 — 정의하는 자리와 배치하는 자리가 다르면 늘린 사람이 배치를 잊어도 조용하다. `content.test.ts` 에 같은 모양의 검사를 하나 더 넣었다.
- 다음에 걸릴 것 같은 것:
  - T-051 보스는 페이즈가 있어 AI 축이 하나 더 늘어난다. 잡몹 프로필에는 페이즈를 두지 않았다 — 잡몹전은 3~6라운드라 전환이 일어날 틈이 없다
  - 지하 맨몸 전멸률이 60% 다. 여기서 적을 더 세게 만들 여지는 없다

### 2026-08-06 · T-062 · 슬롯을 채우러 갔다가 얻을 수 없는 유물 둘을 찾았다
- 한 일: 시작 유물 6 → 8종, 남쪽 안뜰 보상 1등급 → 3등급, 획득 가능성 검사 추가. 테스트 731 → 733개
- 알게 된 것:
  - **유물 12종 중 둘(`deep-lens`, `ember-crown`)이 아무 데서도 나오지 않고 있었다.** 시작 목록에도 회수 지점 넷에도 없어 도감을 채울 방법이 없었다. **도감을 만들면서도 못 봤다** — 도감은 `RELICS` 를 읽고 화면은 멀쩡했으니까.
  - 아무 테스트도 빨개지지 않은 이유는 **데이터가 늘어나는 자리(`relics.ts`)와 데이터를 나눠 놓는 자리(`STARTING_RELICS`·`sites.ts`)가 달라서**다. 둘을 맞춰 보는 검사가 없으면 늘린 사람이 나눠 놓는 것을 잊어도 조용하다. 이제 `content.test.ts` 가 잡는다.
  - **시작 유물을 슬롯보다 많이 줄 수는 없었다.** 유물이 12종뿐이라 아홉을 주면 유적에 남길 것이 셋이고 회수 지점 넷(GDD §6.1)을 못 채운다. 그래서 딱 여덟 — **첫 회수 지점을 줍는 순간 아홉이 되어 "무엇을 뺄까" 가 시작된다.** 조합의 고민을 유적이 주는 편이 처음부터 쥐여주는 것보다 이 게임의 말과 맞다.
  - 유물 여섯 → 여덟이 지하 맨몸 전멸률을 65% → 38% 로 내렸다. 기준(15%) 위라 **위험의 뜻은 유지되고 벽은 아니게 됐다.**
  - ⚠️ **PowerShell 로 소스 파일을 다시 쓰면 한글이 깨진다.** `Get-Content | Set-Content -Encoding utf8` 로 임시 수정을 하려다 `invariants.ts` 주석을 전부 깨뜨렸다. `npm run verify` 는 초록이었다 — 주석이라 컴파일에 지장이 없었기 때문이다. 파일 수정은 Edit 도구로만 한다.
- 다음에 걸릴 것 같은 것:
  - T-050 에서 잡몹을 6종으로 늘리면 인카운터 표를 다시 재야 한다. 지금 값은 "적 종류가 2개" 를 전제로 잡혔다
  - 유물을 13종 이상으로 늘리는 순간 시작 유물을 아홉으로 올릴 수 있다. 그때 `MEASURED_RELICS` 재측정과 함께 본다

### 2026-08-06 · T-059~061 · 보이지 않으면 없는 것과 같다
- 한 일: 적 HP·상태이상 표시, `Esc` 메뉴, 인물 정보 화면. 테스트 729 → 731개, 스모크 13 → 14개
- 알게 된 것:
  - **판정은 다 있는데 화면에 없던 것들이었다.** 상태이상 다섯 가지가 서로 다르게 커맨드를 제약하고, 유물이 스탯을 바꾸고, 적마다 HP 가 다르다 — 전부 코드에서는 동작하는데 플레이어에게는 보이지 않았다. 시스템을 만드는 동안 **불변식이 지켜준 것은 동작이지 전달이 아니었다.**
  - **대상 선택에 근거가 없었다.** 적이 이름만 떠 있어 거의 죽은 적과 온전한 적이 같아 보였다. 대상 선택은 전투의 판단 대부분인데, 그 판단의 입력이 화면에 없었던 셈이다.
  - **`Gauge` 가 부르는 순서에 따라 다른 결과를 냈다.** `setVisible` 과 `setRatio` 가 각자 `fill.setVisible` 을 부르고 있어, 감춘 뒤 비율을 넣으면 다시 나타났다. 상태를 들고 있다가 함께 적용하는 것으로 함정이 사라졌다 — **두 진입점이 같은 것을 쓰면 순서가 곧 버그다.**
  - **단축키를 늘리는 것은 확장 방법이 아니다.** R·F·C·H 넷까지 오니 외울 것만 늘었다. 메뉴를 두되 단축키를 없애지 않고, **메뉴가 단축키를 가르치게** 항목 옆에 키를 적었다.
  - **`?party=full` 이 타이틀의 "새로 시작" 에 지워지고 있었다.** 부팅 때 한 번만 적용해서, 타이틀을 건너뛰는 `?scene=battle` 에서만 듣고 있었다 — 파티를 비우는 곳마다 다시 적용해야 한다. 플래그를 넣은 다음 태스크에서 바로 걸렸다.
- 다음에 걸릴 것 같은 것:
  - 인물 화면이 파티 넷을 창 넘김 없이 그린다. 다섯이 되면 `core/ui/scroll` 을 끼워야 한다 (자리는 주석으로 남겼다)
  - 전투 중에는 메뉴가 없다. 전투에서 인물 정보를 보고 싶어지는 순간이 온다

### 2026-08-06 · T-056~058 · 인원이 늘어난 값이 화면 곳곳에서 뒤늦게 청구서로 왔다
- 한 일: 메뉴 WASD, 저장 키 `F`, 목록 창 넘김, 파티 패널 재배치, 유물 도감. 테스트 712 → 729개, 스모크 11 → 13개
- 알게 된 것:
  - **T-049b 에서 파티를 넷으로 늘린 대가가 화면에서 세 군데로 나왔다.** 전투 패널이 잘리고, 유물 슬롯 목록이 잘리고, 시작 유물이 슬롯보다 적어졌다. 밸런스는 그때 전부 다시 쟀는데 **화면은 아무도 다시 재지 않았다** — 불변식이 숫자만 지키고 있었기 때문이다.
  - **잘린 화면은 예외를 던지지 않는다.** 파티 패널이 y=332 로 화면(270) 밖에 있어도 콘솔은 조용하고 스크린샷은 그냥 두 명만 보인다. 없는 것과 안 보이는 것이 구분되지 않는다 — `data-party-rows` 와 `data-party-bottom` 을 따로 둔 이유다. "넷을 그렸는가" 와 "화면 안에서 끝나는가" 는 다른 질문이다.
  - **스모크가 늘 시작 인원 둘로 돌고 있었다.** 인원에 따라 깨지는 화면을 인원 없이 확인할 수 없는데, 넷을 모으려면 유적을 가로질러야 한다. `?party=full` 로 그 사각지대를 열었다. **테스트가 밟지 않는 상태가 곧 버그가 사는 곳이다.**
  - **패널을 키우는 것은 해법이 아니다.** 유물 열두 종을 여섯 줄 패널에 담고 있었는데, 패널을 열두 줄로 키워도 유물이 스무 종이 되면 같은 일이 벌어진다. 목록 길이는 콘텐츠가 정하고 화면 높이는 480x270 이 정한다 — 둘이 만나는 곳에 창이 있어야 한다.
  - **창을 넘기면 고정 좌표가 전부 거짓말이 된다.** 얼굴 그림을 파티원 순서대로 고정 y 에 두었더니 목록만 넘어가고 얼굴은 제자리에 남아 엉뚱한 줄을 가리켰다. 커서도 같은 함정이었다.
  - 도감에서는 **못 본 유물의 내용을 아예 넘기지 않기로** 했다. `found` 플래그만 두고 가리는 방식은 그리는 쪽이 한 번 빠뜨리면 내용이 샌다. 스크린샷으로는 "가려져 보이는 것" 과 "가려진 것" 이 같아 보인다.
- 다음에 걸릴 것 같은 것:
  - 시작 유물 여섯 < 슬롯 여덟. 백로그 `- [ ]` 로 남겼다 — 늘리면 초반 화력이 오르므로 `maxEarlyWipe` 를 다시 봐야 한다
  - 단축키가 R·F·C·H 넷이 됐다. 화면이 더 늘면 키 하나당 화면 하나가 한계에 부딪힌다. `Esc` 로 여는 메뉴가 필요해지는 시점이 온다

### 2026-08-06 · T-055 · 단축키가 겹쳤다. 안내를 만들려다 배치를 데이터로 뺐다
- 한 일: 저장 키 `S` → `F5`, 키 배치를 `data/keys.ts` 로, `H` 도움말 화면, 겹침 검사 테스트. 테스트 699 → 711개, 스모크 10 → 11개
- 알게 된 것:
  - **`S` 가 아래로 걷기이면서 저장이었다.** 플레이해보면 1분 안에 걸리는데 테스트 700개가 전부 초록이었다 — 각 기능은 잘 돌고, 둘이 같은 키에 매였다는 것만 아무도 안 봤다. **기능 단위 테스트는 기능 사이의 충돌을 못 잡는다.**
  - **안내를 만드는 일이 배치를 데이터로 빼는 일이 됐다.** 도움말 내용을 손으로 적으면 키를 옮긴 사람이 안내까지 고칠 이유가 없다. 실제로 메뉴 화면 세 곳에 `'↑↓ 고르기 Enter 산다 Esc 닫기'` 같은 문장이 하드코딩돼 있었고, 그중 하나는 이미 실제 배치와 어긋나 있었다(`Q` 로도 닫히는데 안내에는 없었다).
  - **겹침은 눈으로 못 잡는다.** 확인해야 할 짝이 키 수의 제곱으로 는다. 배치가 한곳에 모이자 검사가 열 줄짜리 함수가 됐다 — 데이터로 빼는 값어치의 대부분이 여기 있었다.
  - **화면 밖으로 잘린 안내는 없는 것보다 나쁘다.** 여섯 화면의 조작을 한 줄씩 세우니 26줄이라 절반이 잘렸고, 잘린 자리까지가 전부라고 읽힌다. 두 열로 나눴다. 스크린샷을 안 봤으면 `data-help="open"` 은 초록인 채로 넘어갔을 것이다.
  - 덤으로 `LocationBanner` 의 레벨 표시가 컨테이너에서 빠져 있었다. 씬에는 붙어 있어 화면에 뜨긴 했지만 `setScrollFactor(0)` 이 안 걸려 걸을 때마다 카메라를 따라 흘렀다.
- 다음에 걸릴 것 같은 것:
  - 메뉴 화면들이 방향키만 받는다. 탐색에서 WASD 를 쓰던 사람이 메뉴에서는 못 쓴다 — 배치가 한곳에 모였으니 이제 싸게 고칠 수 있다
  - 도움말이 여섯 섹션에서 더 늘면 두 열로도 모자란다. 쪽 넘김이 필요해지는 시점이 온다

### 2026-08-06 · T-049c · 회복을 줄여 소모전을 되찾았다
- 한 일: 레벨업 완전 회복 → 최대치의 70%. 지하 적 레벨 8 → 7, 성장 기준 레벨 10 → 8. 여관 회복 버그 수정. 테스트 697 → 699개
- 알게 된 것:
  - **`it.fails` 가 값을 했다.** T-049b 에서 기준값을 낮추지 않고 빨간 채로 박아뒀기에, 이번에 무엇을 되찾아야 하는지가 명확했다 — 5% 라는 목표가 남아 있었다. 기준을 13% 로 맞춰뒀다면 "지금이 정상" 이 됐을 것이다.
  - **문제는 하나였고 증상이 둘이었다.** 거점이 값을 못 하는 것과 성장이 손해가 되는 것은 같은 뿌리(회복 과잉)에서 나왔다. 회복량 하나를 줄이자 둘 다 사라졌다 — 둘을 따로 고치려 했다면 지하 난이도와 레벨 기준을 각각 비틀었을 것이고, 그건 증상을 옮기는 일이었다.
  - **T-049b 에서 올려둔 값들이 되돌아왔다.** 지하 적 레벨 8 → 7, 성장 기준 10 → 8. 그 값들은 게임을 조정한 게 아니라 **고장 난 상태에서 불변식을 통과시키려고 넓혀둔 것**이었다. 뿌리를 고치니 넓힐 이유가 없어졌다. 회피로 잡은 상수는 원인이 고쳐질 때 같이 지워져야 한다.
  - **여관이 조용히 덜 채우고 있었다.** 기본 스탯으로 회복하고 있어서, 최대 HP 를 올리는 유물을 낀 파티는 자고 나와도 그 유물이 준 만큼이 비어 있었다. 화면에는 "252/270" 으로 보이는데 여관은 제 할 일을 다 했다고 말한다 — **소모전 테스트가 전부 초록이어도 안 잡히는 종류**다. 회복량을 만지러 들어갔다가 발견했다.
- 다음에 걸릴 것 같은 것:
  - `LEVEL_UP_RECOVERY` 와 `LEVEL_CURVE.growth` 는 이제 짝이다. 곡선을 가파르게 하면 회복 간격이 벌어져 성장 역전이 돌아온다
  - 회복 관련 상수가 셋으로 늘었다 (레벨업·승리 MP·여관). 넷째가 생기면 서로 무엇을 상쇄하는지 한 번 정리해야 한다

### 2026-08-06 · T-049b · 파티가 넷이 되자 상수 다섯 개가 함께 움직였다
- 한 일: 파수·탐구자 합류(유적 안에서 만난다), 은신처 구역 2개, 보스 배수·조합 측정 지점·지하 난이도·전투 길이 전면 재측정. 테스트 696 → 697개
- 알게 된 것:
  - **인원은 상수가 아니라 이제 변수다.** 2인으로 시작해 4인이 된다. 그래서 "파티 인원에 맞춘 값" 이라는 개념 자체가 성립하지 않는 자리가 생겼다 — 인카운터 표가 그렇다. 입구는 2인이 지나가고 지하는 3인이 지나간다. 맵마다 다른 인원을 가정하고 잡았다.
  - **적 수는 난이도 손잡이가 아니라 벽 스위치다.** 지하를 3마리 중심으로 올렸더니 맨몸 25% / 자고 나와 28% — 거점이 아무 값도 하지 못했다. 마릿수를 올리면 소모로 지는 게 아니라 **한 판에 몰살당하고**, 한 판 지는 것은 만HP 로 시작해도 못 막는다. 적 레벨로 올려야 단차가 된다.
  - **측정 지점과 표준 전투는 다른 것이다.** 4인 파티 vs 2~3마리는 적 Lv46 까지 올려도 전 조합 승률 100% 였다. 조합 훑기는 갈리는 편성이 필요하고 실제 조우는 그렇지 않다. 훑기가 이미 게임에 없는 적 레벨(37)을 쓰고 있었으므로 적 수도 떼어냈다 — **인위적이어도 되는 자리와 그러면 안 되는 자리를 나눈 것이다.**
  - **회복을 늘린 대가가 이제야 청구서로 왔다.** T-044 의 레벨업 완전 회복과 T-046 의 승리 MP 회복이 겹쳐, 이기는 동안은 자원이 계속 채워진다. 그 결과 (1) 거점이 소모전의 답이 아니게 됐고 (2) **레벨이 높을수록 전멸률이 오르는 구간**이 생겼다 — 경험치 곡선이 지수 1.8 이라 레벨업이 드물어지고, 스탯 이득보다 회복 횟수 손실이 크다. Lv6 → Lv8 에서 25% → 35%, Lv10 부터 뒤집힌다.
  - 그 불변식은 기준을 낮추지 않고 `it.fails` 로 박아뒀다. 건너뛰면 고쳐진 것을 아무도 모르지만, 이 표시는 **고치는 순간 빨개진다.**
- 다음에 걸릴 것 같은 것:
  - T-050 에서 잡몹을 6종으로 늘리면 인카운터 표가 다시 흔들린다. 지금 표는 "적 종류가 2개" 를 전제로 잡힌 값이다
  - `MEASURED_RELICS = 12` 가 여전히 잠금장치로 걸려 있다. 유물을 늘리는 태스크는 측정 지점(적 Lv37)부터 다시 잡아야 한다
  - 위의 `- [!]` 를 고칠 때 `maxEarlyWipe` 를 함께 본다. 회복을 줄이면 T-044 가 답한 "초반이 너무 어렵다" 가 되돌아온다

### 2026-08-06 · T-049a · 파티 인원을 하나의 출처로 — 시뮬레이터가 없는 게임을 재고 있었다
- 한 일: `data/party.ts` 를 인원의 유일한 출처로. 보스 배수·표준 잡몹전·조합 측정 지점 전면 재조정. M5 태스크 생성. 테스트 693 → 696개
- 알게 된 것:
  - **밸런스 시뮬레이터는 4인 파티를 쓰고 있었고, 게임은 2인이었다.** `scenarios.ts` 에 `PARTY_SIZE = 4`, `encounters.ts` 에 2명짜리 `starterParty` — 두 곳에 따로 적혀 있었다. T-044 에서 이 어긋남을 발견하고 소모전 쪽만 고쳤는데, 조합 불변식은 그대로 남아 있었다.
  - 인원을 실제 값으로 맞추자 **보스전 승률이 모든 레벨에서 0%** 가 됐다. 보스 배수(HP 8, 화력 1.4)는 4인 기준이었다. 보스는 액터가 하나라 인원이 적을수록 한 사람이 받는 몫이 커진다 — **화력을 올리면 길어지는 게 아니라 그냥 진다.** HP 는 높이고 화력은 낮추는 쪽(3 / 1.0)이 답이었다.
  - 표준 잡몹전이 3마리였는데 실제로 가장 자주 나오는 조우는 2마리다. **드문 편성으로 "표준" 전투 길이를 재고 있었다.**
  - 조합 측정 지점이 적 Lv40 → Lv32 로 내려왔다. 데이터가 아니라 **재는 대상**이 바뀐 것이다.
  - **라운드 수는 파티 인원에 매인 단위다.** GDD 의 "잡몹 3~6, 보스 12~25" 는 인원을 명시하지 않은 채 쓰였고, 인원이 바뀌자 뜻이 달라졌다. 절대값 대신 **보스/잡몹 비율**을 함께 두었다 — T-049b 에서 4인이 되어도 비율은 그대로 쓸 수 있다.
  - `goodBuildRatio` 를 30% → 40% 로 넓혔다. 30% 로 재면 "가장 좋은 조합에 드는가" 를 묻게 되는데, 이 불변식이 묻고 싶은 것은 "쓰이기는 하는가" 다. 처음 30% 로 잡을 때 "표본이 적은 지금은" 이라고 적어둔 것이 판단의 근거가 됐다.
- 다음에 걸릴 것 같은 것:
  - T-049b 에서 인원이 4명이 되면 슬롯이 8개가 되어 조합 공간이 곱으로 커진다. 측정 지점을 또 다시 잡아야 하지만, 이번엔 배선이 인원을 따라가므로 숫자만 재면 된다.
  - 통과 구간이 2개 레벨로 좁다. 계속 좁아지면 측정이 불안정해진다.

### 2026-08-06 · T-041b · 상점 — **M4 완료**
- 한 일: `core/world/shop.ts`(판정), `ShopScene`(목록·커서·구매), 소지 한도 9, 전투 아이템 5종 전부 판매. 테스트 672 → 693개
- 알게 된 것:
  - **값을 매길 기준점이 필요했다.** 절대적인 "약초는 얼마가 적당한가" 는 답이 없다. 여관(파티 전원 완전 회복 = 은편 22)을 기준으로 삼으니 전부 풀렸다 — 아이템은 회복량이 적은 대신 **전투 중에 쓸 수 있고**, 그 차이가 값의 근거다.
  - `잿불`(부활)을 **여관보다 비싸게** 잡았다. 전멸은 되돌릴 수 없는 유일한 것이라, 싸면 "죽으면 일으키지" 가 정답이 되어 소모전이라는 축이 사라진다. 테스트로 못 박았다.
  - **스모크를 "살 수 있다" 가 아니라 "은편이 모자라면 거절한다" 로 썼다.** 처음에는 전투로 은편을 벌어 사는 흐름을 짰는데, 그건 승패에 매인 불안정한 테스트이고 실패해도 상점 탓인지 전투 탓인지 알 수 없다. 거절 경로는 새 게임에서 결정적이고, 화면·판정·소지품이 이어져 있는지는 똑같이 검증한다.
  - 화면 세 개(유물·세이브·상점)가 같은 조작을 쓴다. 목록·커서·Enter·Esc — 화면마다 다르면 플레이어가 매번 다시 배워야 한다.
  - 세이브 화면에서 배운 것(항목마다 별도 Text 를 정해진 y 에)을 그대로 썼다. 커서 위치를 계산하면 줄 수가 달라지는 순간 틀린다.
- 다음에 걸릴 것 같은 것:
  - **M5(수직 슬라이스)가 남았다.** 지금까지는 시스템을 만들었고, 이제 그 시스템으로 실제로 플레이할 한 판을 만드는 일이다.
  - T-048(확인 창)이 아직 남아 있다 — 여관은 여전히 묻지 않고 값을 치른다.

### 2026-08-06 · T-041a · 통화와 여관 — 거점이 소모전의 답이 됐다
- 한 일: 은편(통화), 적 처치 보상, 여관(HP·MP 완전 회복). 세이브 v4. 새 불변식 둘. 테스트 670 → 672개. T-041 을 여관/상점으로 쪼갰다
- 알게 된 것:
  - **거점을 쓰면 지하 전멸률이 33% → 0%** 가 된다. 소모전 불변식이 "준비 없이 내려가면 위험하다" 를 지킨다면, 새 불변식은 **"준비하면 넘을 수 있다"** 를 지킨다. 하나만 있으면 게임은 막다른 길이거나 산책이다.
  - **첫 보상 수치가 너무 후했다.** 15판을 돌면 여관 값의 일곱 배가 남아 은편이 자원 노릇을 못 했다. 절반으로 줄여 세 배쯤으로 맞췄다 — 상점이 들어설 여유는 있고 흔하지는 않은 구간이다.
  - 여기서 더 조이지 않은 이유: **살 것이 없는 상태에서 가격을 정하는 것은 근거 없는 일이다.** 상점(T-041b)이 붙으면 다시 잰다.
  - 새 불변식 **"벌이가 숙박비를 감당한다"** 는 난이도가 아니라 **막다른 길**을 막는다 — 잘 돈이 없으면 회복하려면 싸워야 하고 싸우려면 회복해야 하는 상태에 갇힌다. 은편을 조이면 여기가 먼저 빨개진다.
  - 여관은 HP·MP 만 돌려주고 침식은 건드리지 않는다. 한 자리에서 모든 것이 해결되면 다른 자리에 갈 이유가 없어진다.
- 다음에 걸릴 것 같은 것:
  - 여관이 **묻지 않고 바로 값을 치른다.** 대화 시스템에 선택지가 없어서인데, 상점도 같은 것이 필요하므로 T-048 로 묶었다.
  - 상점(T-041b)이 M4 의 마지막이다.

### 2026-08-06 · T-047 · 정화소 재측정 — 답이 뒤집혔다
- 한 일: T-046 이후 정화소를 다시 쟀다. 새 불변식 둘(침식이 실제로 물린다 / 거점에 들르면 봉인이 줄어든다). 테스트 667 → 669개
- 알게 된 것:
  - **같은 질문에 두 번 다른 답이 나왔고, 둘 다 맞았다.**

    ```
    지하 Lv8 전멸률      정화 안 함   5판마다
      T-045 (MP 병목)        3%         2%
      T-047 (지금)           7%         1%
    ```

    T-045 에서는 MP 가 먼저 말라 스킬을 못 쓰니 침식이 쌓일 일도 없었다. T-046 에서 MP 회복이 붙어 스킬을 계속 쓰게 되자 침식이 비로소 비용이 됐고, 그러자 정화소가 일을 하기 시작했다.
  - **측정 결론은 그것을 잰 시점의 다른 값들에 매여 있다.** T-045 의 결론이 틀린 게 아니라, 그 결론이 기대던 조건이 바뀌었다. 그래서 재측정을 백로그에 남겨둔 것이 그대로 값을 했다 — "이건 지금 조건에서만 참" 이라는 것을 그때 적어두지 않았다면 다시 재볼 생각을 못 했을 것이다.
  - 불변식을 **짝으로** 세웠다. 가동률은 "MP 가 덜 물려야 한다" 는 상한이고, 봉인율은 "침식이 물려야 한다" 는 하한이다. 한쪽만 있으면 자원 하나를 없애는 방향으로 흘러도 초록이다.
  - 봉인율을 지표로 고른 이유는 **전멸률보다 표본이 많아 덜 흔들리기 때문**이다 (시행 40회가 아니라 전투 600회). 작은 확률을 적은 시행으로 재면 판정이 잡음에 매인다.
- 다음에 걸릴 것 같은 것:
  - M4 에 T-041(상점·여관)만 남았다. 여관이 HP 를 돌려주면 소모전 축이 또 움직인다 — 그때 `ATTRITION` 을 다시 재야 한다. 이번에도 재측정 태스크를 함께 남긴다.

### 2026-08-06 · T-046 · MP 회복 — 유물 시스템이 다시 켜졌다
- 한 일: 승리 시 쓰러뜨린 적 하나당 MP 1 회복. 승리 정산을 `partyStore.settleVictory` 한 곳으로 모았다. 가동률 불변식 55% → 85%. 테스트 661 → 667개
- 알게 된 것:
  - **가장 작은 단위가 이미 충분했다.** 적 하나당 1 이면 가동률이 61~65% → 98% 로 뛴다. 그 이상 줘도 98% 에서 포화한다 — 문제는 "MP 가 모자라다" 가 아니라 **"회복 경로가 아예 없다"** 였다.
  - 비율(최대 MP 의 N%)로 주려다 그만뒀다. 4% 면 MP 1 인데, 플레이어에게 "MP 1 회복" 은 보이지 않는 값이다. 적 수에 비례하는 쪽이 알아보기 쉽고 "크게 싸웠으니 크게 돌아온다" 가 자연스럽다.
  - **지하 전멸률이 3% → 7% 로 올랐고, 이건 좋은 신호다.** MP 가 마르지 않으니 스킬을 쓰고, 스킬을 쓰니 침식이 쌓인다. GDD §6.2 가 정한 "MP 는 보조, 침식이 주 자원" 이 그제서야 사실이 됐다.
  - **HP 는 돌려주지 않았다.** 둘 다 돌려주면 소모전이라는 축 자체가 사라진다. 그건 거점의 몫이다.
  - T-045 에서 55% 를 두며 "목표가 아니라 바닥" 이라고 적어둔 것이 그대로 쓸모가 있었다. 무엇을 바꿔야 하는지와 얼마나 올려야 하는지가 이미 문서에 있었다.
  - 승리 정산을 한 함수로 모았다. T-044 에서 시뮬레이터가 4인 파티를 재는 동안 게임은 2인이었던 일이 있었는데, 정산이 흩어져 있으면 그런 어긋남이 또 생긴다.
- 다음에 걸릴 것 같은 것:
  - 이제 침식이 실제 비용이 됐으니 **정화소의 값어치가 달라졌을 수 있다.** T-045 에서 "정화소는 기준선을 못 바꾼다" 고 쟀는데, 그건 MP 가 병목이던 시절의 측정이다. 다시 재볼 만하다.
  - 남은 M4 는 T-041(상점·여관)뿐이다.

### 2026-08-06 · T-045 · 소모전 재측정 — 정화소는 답이 아니었다
- 한 일: 거점이 생긴 뒤의 소모전을 다시 쟀다. 기준선은 **바꾸지 않았다** — 바꿀 이유가 없다는 것이 측정 결과였다. 대신 진짜 병목을 불변식으로 만들었다. 테스트 659 → 661개
- 알게 된 것:
  - **정화소는 소모전을 거의 바꾸지 못한다.** 매 판 전에 정화한다는 물리적으로 불가능한 상한을 줘도 지하 전멸률이 35% → 16% 에 그친다. 현실적인 5판마다는 35% → 31% 다. 파티를 죽이는 것은 침식이 아니라 **HP·MP** 였다.
  - 그래서 T-045 의 결론은 "기준선을 갱신했다" 가 아니라 **"갱신할 필요가 없었고, 그 이유가 다음 할 일을 가리킨다"** 다.
  - **진짜 문제는 유물 시스템이 멈춘다는 것이다.** 지하 소모전에서 전투의 35~39%가 **아무도 스킬을 못 쓰는 상태로 시작한다.** 능력이 유물에서 나오는 게임인데(ADR-004) 그 구간에서는 기본 공격만 남는다.
  - 판별 추이가 그 모양을 그대로 보여준다 — 스킬 가능 인원 `2 2 2 2 1 0 0 0` 이었다가 레벨업에서 `2` 로 돌아오고 다시 `2 2 2 1 1 0 0`. MP 는 레벨업 말고 회복 경로가 없다.
  - **승률만 보면 이 문제가 안 보인다.** 기본 공격만으로도 이기기는 한다. 그래서 가동률을 별도 불변식으로 세웠다 — 이 게임이 "유물 게임" 으로 굴러가고 있는지를 재는 유일한 수치다.
  - 새 불변식의 바닥값(55%)은 **목표가 아니라 바닥**이다. 주석과 백로그에 그렇게 못 박았다 — 숫자만 남으면 나중에 "이 정도면 됐다" 로 읽힌다.
- 다음에 걸릴 것 같은 것:
  - T-046(MP 회복 수단)이 가동률을 올린다. 여관·전투 후 소량 회복·회복 아이템 중 어느 쪽이든 소모전 기준선이 함께 움직이므로 그때 둘 다 다시 재야 한다.

### 2026-08-06 · T-040 · 거점 맵과 정화소
- 한 일: `haven.tmj`(40x24, 통째로 안전지대), `core/world/facility.ts`(시설 판정·배치 검증·정화 계산), `data/facilities.ts`, 야영지 구석에서 오가는 길. 테스트 638 → 659개
- 알게 된 것:
  - **맵을 손으로 쓰지 않고 생성했다.** 40x24 CSV 를 두 겹(ground·collision) 손으로 적으면 반드시 한 칸이 어긋나고, 그건 화면을 봐야만 드러난다. 생성기를 짜고 ASCII 로 찍어 확인했다.
  - **정화소 하한을 정화석 값에서 가져온다.** 처음엔 30이라고 따로 적었는데, 침식 40 구간에서 정화석(고정 40)이 앞서는 것을 테스트가 잡았다. 두 숫자를 따로 적으면 언젠가 한쪽만 바뀌고 "거점이 더 나쁜" 구간이 조용히 생긴다.
  - 비율로 씻는 것이 핵심이다 — 고정량이면 침식이 많이 쌓일수록 정화석과 차이가 없어지고, 그러면 거점까지 걸어올 이유가 사라진다.
  - **타입이 설계 구멍을 잡았다.** `MapId` 에 거점을 넣자 `ENCOUNTER_TABLES`·`AREA_LEVELS`·`SITES_BY_MAP` 이 전부 컴파일 에러를 냈다. 빈 표를 넣어 넘길 수도 있었지만 `Partial` 로 바꾸는 쪽을 택했다 — 빈 표는 "전투가 있는데 편성이 비었다" 로도 읽히지만, 항목이 없는 것은 "여기서는 싸우지 않는다" 로만 읽힌다.
  - 시설은 NPC 처럼 길을 막는다. 밟고 지나갈 수 있으면 마주 보고 말을 거는 조작이 성립하지 않는다.
- 다음에 걸릴 것 같은 것:
  - **소모전 기준선(`ATTRITION`)을 다시 재야 한다.** 정화소가 생겨 회복 수단이 늘었는데, 지금 불변식은 "회복은 레벨업뿐" 이던 시절의 값이다. 거점 왕복을 포함한 측정이 필요하다.
  - 상점·여관(T-041)이 붙으면 거점 왕복이 실제 전략이 된다. 그때 유적 왕복 비용(걸음 수)도 함께 봐야 한다.

### 2026-08-05 · T-044 · 레벨업과 초반 난이도 — 단판 측정의 한계가 드러났다
- 한 일: `core/progress/level.ts`(경험치·레벨 곡선), 레벨업 시 완전 회복, 지역 레벨·인카운터 간격 재조정, `tests/balance/attrition.test.ts`. 세이브 v2 → v3. 테스트 616 → 638개
- 알게 된 것:
  - **단판 승률 100% 인데 연속으로는 평균 6판 만에 전멸하고 있었다.** 사용자가 "처음부터 너무 어렵다" 고 했을 때 시뮬레이터는 승률 100%, 남은 HP 88% 를 보여줬다. 시뮬레이터가 틀린 게 아니라 **재는 대상이 틀렸다** — 유적에는 회복 수단이 없어 HP·MP·침식이 판을 넘어 쌓이는데, 그때까지의 측정은 전부 단판이었다.
  - 실측 소모전: HP 76→51→33→7→0, **MP 는 3판째 고갈**. MP 가 마르면 기본 공격만 남아 그때부터는 유물도 조합도 의미가 없다.
  - **시뮬레이터는 4인 파티로 재고 있었는데 게임은 2인이다.** 밸런스 시나리오(`scenarios.ts`)의 `PARTY_SIZE = 4` 가 실제 게임과 어긋나 있었다.
  - **적 수가 레벨보다 훨씬 세게 먹힌다.** 파티 Lv6·적 Lv5 기준 2마리면 전멸 1%, 3마리면 100%. 파티가 2인뿐이라 행동 수 비율이 그대로 화력 차이가 된다. 지하 인카운터 표가 3~4마리 중심이었던 것이 문제였다.
  - 레벨업을 **완전 회복과 묶은 것**이 핵심이었다. 유적 안에 회복 수단이 없는 상태에서 "한 판만 더 버티면 오른다" 가 유일한 숨통이다. 이게 없으면 곡선을 아무리 만져도 소모전은 결국 진다.
  - 조정 후: 입구 Lv1 시작 → 25판 전멸 0%, Lv6 도달 / 지하 Lv6 → 35% / 지하 Lv8 → 3%.
  - **T-019 의 마지막 `it.todo` 가 사라졌다.** "유적 1회 완주에 정화 1회 필요" 보다 "회복 없이 몇 판 버티는가" 가 지금 실제로 물어야 할 것이다.
- 다음에 걸릴 것 같은 것:
  - 소모전 불변식이 생겼으니 앞으로 밸런스를 만질 때 이쪽이 먼저 빨개질 것이다. 단판 불변식보다 민감하다.
  - 거점(T-040·T-041)이 생기면 회복 수단이 늘어 소모전 기준선이 통째로 움직인다. `ATTRITION` 을 그때 다시 재야 한다.

### 2026-08-05 · T-039 · 유물 회수 지점 — 첫 실제 마이그레이션
- 한 일: `core/world/site.ts`, `data/sites.ts`(맵마다 2개), 밟으면 줍고 유물의 기록을 대화로 보여준다. **세이브 v1 → v2** (`collectedSites`). 테스트 604 → 614개
- 알게 된 것:
  - **T-037 에서 미리 만들어 둔 마이그레이션 기제가 처음으로 쓰였다.** 스키마를 올리자 v2 를 기대하는 검증이 옛 픽스처를 거부했고, 그게 정확히 설계한 동작이다. 기제를 그때 만들었으면 급하게 만들었을 것이다.
  - 옛 세이브를 **"아무것도 줍지 않았다" 로 올린다.** "전부 주웠다" 로 보면 아직 못 가본 층의 유물을 영영 잃는다. 다시 주울 수 있는 것은 손해가 아니라 이득이므로 이쪽이 안전한 방향이다.
  - **주웠다는 사실이 세이브에 남지 않으면 저장·로드 반복이 유물 무한 획득이 된다.** 회수 지점을 만들 때 진짜 어려운 부분은 화면이 아니라 이것이었다.
  - 회수 지점을 **위험 구역에만** 둔다는 규칙을 테스트로 박았다. 안전지대에 두면 전투를 거치지 않고 유물이 늘어나 유적에 들어갈 이유가 없어진다.
  - 계단·NPC 와 같은 칸에 놓는 것을 검증이 막는다. 밟는 순간 둘 다 발동하려 들고, 어느 쪽이 이기는지는 코드 순서에 달리게 된다 — 화면에서 보고 알아낼 수 없는 종류의 버그다.
- 다음에 걸릴 것 같은 것:
  - **로컬 Playwright 가 크로미움을 띄우지 못한다** (`spawn UNKNOWN`). 이 세션 안에서 잘 돌다가 갑자기 시작됐고, 바이너리는 멀쩡하다. CI 스모크는 정상이라 진행에는 지장이 없지만 로컬 화면 확인이 막혔다 — 백로그에 사람 몫으로 올렸다.
  - 남은 회수 지점용 유물이 둘(깊은 렌즈·잿불 관) 있다. 맵이 늘어나는 M5 에서 놓을 자리가 생긴다.

### 2026-08-05 · T-038 · 세이브 슬롯 3개와 localStorage
- 한 일: `game/save/storage.ts`(어댑터), `SaveScene`(저장·불러오기 겸용), `game/playtime.ts`. 타이틀 `C` 이어하기, 탐색 중 `S` 저장. 테스트 584 → 604개, 스모크 6 → 7개
- 알게 된 것:
  - **읽기는 절대 던지지 않게 만들었다.** 슬롯 상태를 값(`empty`/`ok`/`broken`)으로 돌려주고 화면이 그대로 보여준다. 깨진 슬롯을 빈 슬롯처럼 감추면 플레이어는 세이브가 사라졌다고 생각하고, 덮어쓴 뒤에는 되돌릴 수 없다.
  - **테스트가 실수를 하나 잡았다.** 범위 밖 슬롯 번호가 try 안에 있어서 "깨진 세이브" 로 둔갑했다. 코드 문제를 데이터 문제로 감추면 멀쩡한 세이브가 깨진 것처럼 보인다 — 번호 검사를 try 밖으로 뺐다.
  - 저장소를 주입받게 만든 덕에 실패 경로를 전부 헤드리스로 잴 수 있다. 용량 초과·SecurityError·깨진 JSON·미래 버전·없는 콘텐츠 참조 — **테스트 20개 중 대부분이 실패 경로다.** 성공 경로는 한 번만 확인하면 되지만 실패는 저마다 다르게 틀어진다.
  - 슬롯 목록을 한 덩어리 Text 로 그렸더니 커서가 어긋났다 — `ok` 는 두 줄, `empty` 는 한 줄이기 때문이다. 슬롯마다 별도 Text 를 정해진 y 에 두어 계산이 아니라 **구조로** 맞췄다.
  - 스모크가 **페이지 새로고침을 건너** 확인한다. 같은 세션 안에서만 되돌아오는 것은 메모리 상태를 복사한 것일 뿐이라, localStorage 를 실제로 거쳤다는 증거가 아니다.
  - 저장 성공은 "저장했다" 알림이 아니라 **슬롯이 `empty` 에서 `ok` 로 바뀌는 것**으로 판정한다. 알림은 실패해도 띄울 수 있다.
- 다음에 걸릴 것 같은 것:
  - 전멸 시 지금은 타이틀로 간다. 세이브가 생겼으니 "마지막 저장 지점에서 다시" 가 선택지로 붙어야 자연스럽다.
  - 플레이 시간이 씬 전환마다 정산되지 않는다. 타이틀에서 시작할 때만 시계를 돌린다 — 전투 중 시간이 새는지 확인 필요.

### 2026-08-05 · T-037 · 세이브 스키마와 마이그레이션 골격
- 한 일: `core/save` — 스키마, 마이그레이션 체인, 교차 참조 검사. `partyStore` 에 `captureSave`·`restoreSave`. 유물 소지 목록이 상수에서 상태로 바뀌었다. 테스트 549 → 584개
- 알게 된 것:
  - **마이그레이션이 아직 하나도 없어서 기제를 가짜 버전으로 검사했다.** T-027 의 기구 검사와 같은 문제다 — 처음 필요해지는 순간이 곧 처음 시험하는 순간이 되면 안 되고, 세이브는 그때 이미 남의 파일이 존재한다.
  - **월드 난수는 시드가 아니라 상태를 저장한다.** 시드만 저장하면 불러올 때마다 인카운터 수열이 처음부터 다시 시작해, 같은 자리에서 저장·로드를 반복하는 것으로 조우를 조작할 수 있다. `getState`/`restoreRng` 가 M0 부터 있었던 것이 여기서 값을 했다.
  - **미래 버전 세이브를 거부한다.** 옛 빌드가 새 세이브를 열면 모르는 필드를 조용히 버리고, 다시 저장하면 데이터가 사라진다. 여는 것을 막는 편이 싸다.
  - 구조 검증과 **교차 참조 검증을 나눴다.** core 는 어떤 유물이 존재하는지 모른다(ADR-001) — 호출부가 알려준다. 콘텐츠를 지우면 옛 세이브가 없는 유물을 가리키는데, 조용히 넘어가면 장착 화면에서 터진다.
  - 왕복 테스트는 **JSON 을 거쳐서** 한다. 직렬화되지 않는 값이 섞이면 그때 걸린다.
- 다음에 걸릴 것 같은 것:
  - T-038 에서 localStorage 가 붙는다. **깨진 JSON·용량 초과·비활성 저장소** 셋 다 실제로 일어나고, 그때 게임이 멈추면 안 된다.
  - `SAVE_VERSION` 을 올리는 순간 "마이그레이션이 빈틈없이 이어진다" 테스트가 빨개진다. 그게 목적이다.

### 2026-08-05 · T-029 · 유물 장착 UI — **M3 완료**
- 한 일: `RelicScene`. 슬롯↔유물 두 칸 이동, 장착/해제, 유물 상세, **공명 실시간 표시**. 탐색 중 `R` 로 열고 `Esc` 로 제자리 복귀. 시작 유물 2 → 6종. 테스트 548 → 549개, 스모크 4 → 6개
- 알게 된 것:
  - **성립한 공명만 보여주면 안 된다.** 아직 아닌 공명과 무엇이 몇 개 더 필요한지를 함께 띄운다 — 성립한 것만 보이면 조합을 우연히 발견해야 하고, 이 게임에서 그건 핵심 시스템을 숨기는 일이다. GDD §6.4 에 명문화했다.
  - **시작 유물이 슬롯보다 적으면 장착 화면에 고를 것이 없다.** 2종/4슬롯이라 전부 끼는 것이 유일한 답이었다. 6종으로 늘렸고, 기존 테스트("시작 유물이 슬롯을 넘지 않는다")는 뜻이 정반대로 뒤집혔다.
  - **테스트가 데이터 개수를 하드코딩하고 있었다.** `partyStore` 테스트 다섯 개가 "시작 유물 2종" 을 전제하고 깨졌다. 개수가 아니라 성질(빈 슬롯 없이 시작한다 / 뺀 유물의 스킬이 사라진다)을 재도록 고쳤다 — 데이터가 늘 때마다 깨지는 테스트는 신호가 아니라 소음이다.
  - 장착 화면을 거점 전용으로 두지 않았다. 유적 안에서 조합을 못 바꾸면 조합이 게임의 중심이라는 말과 어긋난다.
  - 스모크가 **되돌리기까지 확인한다** — 빼면 공명이 사라지고, 도로 끼우면 원래대로 온다. 한쪽으로만 맞는 판정은 절반만 맞는 것이다.
- 다음에 걸릴 것 같은 것:
  - M4(거점·세이브)로 넘어간다. 정화소·상점·여관과 localStorage 세이브. 세이브 스키마에 유물 숙련도와 침식이 들어가므로 마이그레이션 규칙(CLAUDE.md 7)이 처음으로 실제 의미를 갖는다.
  - 유물 획득 기제가 없어 시작 유물을 손으로 쥐여주고 있다. M4 의 회수 지점이 이 자리를 대신해야 한다.

### 2026-08-05 · T-028b · 유물 12종 + 공명 4종 (수직 슬라이스 분량)
- 한 일: 유물 7 → 12종, 스킬 8 → 12종(독·혼란 추가), 공명 3 → 4종. GDD §8 의 수직 슬라이스 분량을 채웠다
- 알게 된 것:
  - **불변식 셋이 전부 한 번에 통과했다.** 유물 5종을 한꺼번에 넣었는데 격차 2.7%p, 밴드 55.0%, 필수도 최대 2.7%p. T-035 에서 배운 규칙(최강 액티브는 0단계에 두지 않는다, 등급별 침식 계수 띠를 겹치지 않는다)을 처음부터 지키며 설계한 결과다 — **불변식이 사후 검사가 아니라 설계 지침이 됐다.**
  - 조합이 495개가 되어 `thinBuilds` 솎아내기가 처음으로 실제 동작한다. 리포트에 "495개 중 120개" 로 남는다 — 조용히 자르면 "전부 훑었다" 로 읽힌다.
  - 채택률이 8.3~61.1% 로 넓게 퍼졌다. 유물이 적을 때는 채택률이 조합 열거 방식에 끌려다녔는데, 12종쯤 되니 실제 값어치를 반영하기 시작한다.
  - **통과 구간이 좁아졌다.** 7종일 때 3개 레벨(39~41)이 통과했는데 12종에서는 2개(40~41)다. 유물이 늘수록 조합끼리 비슷해져 승률이 몰린다.
- 다음에 걸릴 것 같은 것:
  - **통과 구간이 계속 좁아지면 측정이 불안정해진다** (T-036). 난이도 곡선이 가파른 것(적 +2 레벨에 전승↔전패)과 같은 뿌리로 보인다.
  - 남은 M3 태스크는 T-029(장착 UI) 하나다. 유물 12종·공명 4종을 화면에서 고를 수 있어야 이 시스템이 실제로 플레이어에게 닿는다.

### 2026-08-05 · T-035 · 필수 유물 불변식 — 넣자마자 무언가를 잡았다
- 한 일: 불변식 4b(필수 유물 부재) 추가. `exclusionPenalty` — 그 유물을 빼고 짠 최고 조합이 전체 최고보다 얼마나 나쁜가. 테스트 543 → 548개
- 알게 된 것:
  - **채택률에 상한을 두지 않고 직접 물어봤다.** 상한은 "상위 몇 %를 좋은 조합으로 볼 것인가" 라는 임의의 기준에 결과가 매달린다. "이 유물을 빼고도 비슷하게 셀 수 있는가" 는 그렇지 않다.
  - **넣자마자 `잿빛 등` 을 잡았다** — 빼면 26.7%p 손해. 원인은 위력 170 짜리 액티브를 0단계에 둔 것이었다. 다른 유물의 0단계 액티브는 110~152 였으니 급이 달랐다. 2단계로 잠그니 채택률 100% → 18.2% 로 떨어졌다.
  - **데이터를 고치니 측정 지점도 따라 움직였다.** 파티가 약해지자 Lv42 에서 전부 지게 되어 밴드가 74% → 9% 로 무너졌다. Lv40 으로 내리고 Lv39~41 구간이 모두 통과하는 것을 확인했다.
  - 불변식을 **콘텐츠보다 먼저** 넣은 것이 맞았다. T-028b 에서 유물 5종을 더 넣은 뒤에 이걸 넣었으면, 필수 유물이 생긴 원인이 어느 유물 때문인지 알 수 없었을 것이다.
- 다음에 걸릴 것 같은 것:
  - 측정 지점이 데이터를 고칠 때마다 움직인다. 지금은 손으로 다시 재는데, 유물이 12종이 되면 이 왕복이 비싸진다 — 측정 지점을 자동으로 찾는 도구가 필요해질 수 있다.
  - `gale-fang` 채택률 90.9%. 필수도는 통과하지만(17%p) 눈여겨볼 자리다.

### 2026-08-05 · T-028a · 유물 7종 — 불변식이 처음으로 실제 데이터를 지켰다
- 한 일: 유물 3 → 7종, 스킬 3 → 8종. 측정 지점 재조정(ADR-013 정정), 침식을 실효 위력에 비례시킴(ADR-014). 테스트 531 → 541개
- 알게 된 것:
  - **T-027 의 `측정의 한계` 테스트가 설계대로 빨개졌고, 그게 옳았다.** 옛 측정 지점(적 Lv36)에서는 슬롯을 채운 조합이 **전부 100% 로 이겨** 아무것도 갈리지 않았다. 그 테스트가 없었으면 "불변식이 통과했다" 며 지나갔을 것이다.
  - **어떤 난이도에서도 모든 조합 크기가 동시에 갈리지 않는다.** 유물 1개짜리와 4개짜리는 애초에 비교 대상이 아니다. 측정 대상을 **슬롯을 채우되 가진 것을 다 끼지는 못하는 조합**으로 좁혔다 — 그게 플레이어가 실제로 고르는 조합이다.
  - **이번엔 구간을 확인했다.** Lv41·42·43 이 모두 통과한다. 지난번(유물 3종)에는 한 점만 보고 골랐는데, 한 레벨만 통과하면 그건 측정이 아니라 우연을 고른 것이다.
  - **불변식이 데이터를 따라오지 않고 데이터가 불변식을 고치게 했다.** 상태이상 스킬을 넣자 "위력이 높을수록 침식도 크다" 가 깨졌다 — `공허 물기` 는 위력이 가장 낮은데 침묵을 건다. 숫자를 만져 통과시킬 수도 있었지만, 그러면 상태이상 스킬이 침식당 값어치 1위가 되어 불변식이 막으려던 상태가 된다. 실효 위력 개념을 도입해 불변식 쪽을 고쳤다 (ADR-014).
  - 실측(파티 Lv20 vs 잡몹 Lv42, 시드 300, 조합 35개): 격차 6.7%p, 밴드 57.1%, 승률 89%~8%. 사장된 유물 없음.
- 다음에 걸릴 것 같은 것:
  - **`ash-lantern` 채택률이 100% 다.** 좋은 조합 전부에 낀다. 지금 불변식 4 는 하한만 보므로 안 잡힌다 — 필수 유물을 잡는 상한 불변식이 필요하다 (T-035).
  - `AILMENT_POWER` 는 근거가 약한 초기 추정이다. 상태이상을 포함한 전투를 충분히 돌려봐야 안다.
  - 조합 훑기가 verify 시간의 큰 몫을 차지하기 시작했다 (밸런스 테스트 ~5초). 12종이면 495조합이라 솎아내기가 실제로 필요해진다.

### 2026-08-05 · T-027 · 유물 밸런스 불변식 (M3 관문)
- 한 일: `core/relic/builds.ts`(조합 열거·크기 묶기·순위 격차·채택률), `core/battle/sweep.ts`(편성끼리 비교), `data/invariants.ts`(GDD §5.5 수치), `tests/balance/relicBuilds.test.ts`. T-019 의 `it.todo` 3개가 실제 테스트가 됐다. 테스트 496 → 531개
- 알게 된 것:
  - **처음 잰 결과는 실패였다 (26.7%p / 상한 25%p).** 그런데 격차의 정체는 1위가 유물 3개 조합, 5위가 1개 조합이라는 것이었다 — **어느 유물이냐가 아니라 몇 개냐**의 차이였다. 슬롯 4개에 유물 3종이라 슬롯이 남고, 조합에 기회비용이 없다. 크기가 같은 조합끼리 비교하도록 고쳤다 (ADR-013).
  - **시드 수가 판정을 뒤집었다.** 시드 40으로 재니 22%p 로 통과, 300으로 재니 26.7%p 로 실패. 표본이 모자라 통과하는 불변식은 초록일 때 더 위험하다. 하마터면 그대로 머지할 뻔했다 — `npm run sim` 이 300시드로 돌면서 다른 숫자를 뱉어준 덕에 잡혔다.
  - **불변식에 기구 검사를 붙였다.** 지배 전략과 사장된 유물을 일부러 심어 불변식이 잡아내는지 본다. 이게 없으면 초록이 "문제가 없다" 는 뜻인지 "아무것도 재지 않았다" 는 뜻인지 알 수 없다. 실제로 심어보니 격차가 69.3%p 로 튀어 잡혔다.
  - 채택률만 크기를 섞어서 잰다. `잿불 고리` 는 단독 48.0% 로 꼴찌지만 좋은 조합의 절반에 낀다 — 크기별로 재면 사장됐다고 잘못 판정된다.
- 다음에 걸릴 것 같은 것:
  - **T-028 에서 `측정의 한계` 테스트가 일부러 빨개진다.** 유물이 슬롯을 넘으면 측정 지점을 다시 잡아야 한다.
  - 유물 12종이면 조합이 793개다. `thinBuilds` 가 있지만 솎아낸 사실을 리포트에 남겨야 한다.
  - 난이도 곡선이 가파르다 — 적 레벨 +14 에서 전부 이기고 +19 에서 전부 진다. 불변식과 별개의 문제지만 언젠가 다뤄야 한다.

### 2026-08-05 · T-033 · 인물 스프라이트
- 한 일: 사용자가 조달한 Kenney Roguelike Characters(CC0)를 벤더 팩으로 등재. `data/characters.ts` 가 사람 프레임을 전담하고, 파티·NPC·전투 초상이 전부 여기서 나온다. 색인 스키마에 `frame.spacing` 추가. 테스트 491 → 496개
- 알게 된 것:
  - **이 팩에도 4방향은 없었다.** 종이인형 방식(몸통 + 옷 + 모자 + 무기)의 **정면 한 방향**이고, 6~11행 두 열만 이미 합쳐진 인물이다. 합성 기제가 없는 지금 쓸 수 있는 것은 그 14명뿐이다.
  - 시트마다 여백 규칙이 다르다. Kenney 의 `_packed` 는 여백이 없지만 이 팩은 1px 씩 띄운다. **여백을 틀리면 화면이 비지 않고 한 칸씩 밀린 그림이 나온다** — 에셋이 없을 때보다 알아채기 어려워서, 선언한 격자와 실제 PNG 크기를 대조하는 테스트를 넣었다.
  - 사람을 한 시트로 모았다. 사람마다 다른 시트를 쓰면 "이 번호가 어느 시트의 것인지" 를 매번 따져야 하고, 그건 반드시 틀린다.
  - 전투가 프론트뷰인데 파티 쪽에 그림이 하나도 없었다. 초상을 넣으려고 게이지를 20px 씩 밀었다 — 레이아웃은 테스트가 못 잡으므로 스크린샷으로 확인했다.
- 다음에 걸릴 것 같은 것:
  - 적은 아직 `tiles-dungeon` 프레임을 쓴다 (T-034). 두 시트를 섞어 쓰는 상태가 오래가면 번호를 헷갈리게 된다.
  - 4방향 걷기는 여전히 사람 몫이다. 지금 팩으로는 불가능하다.

### 2026-08-05 · T-026 · 유물별 침식 계수
- 한 일: `activesOf` 가 유물 계수를 스킬 침식량에 곱한다(ADR-012). 공명 효과 종류를 넓혀 `erosionRelief` 추가 — T-024 에서 미뤄둔 것을 소비 기제가 생긴 지금 붙였다. 측정용 `relicFight` 시나리오와 `npm run sim` 유물별 표. 테스트 474 → 491개
- 알게 된 것:
  - **침식 계수를 물릴 자리가 전투 계층이 아니었다.** 침식을 쌓는 쪽은 유물을 모르고, 알게 만들면 시뮬레이터가 유물 없이 못 돈다. 유물이 스킬을 내줄 때 조정된 사본을 만드니 전투 계층이 한 줄도 안 바뀌었다.
  - 같은 스킬을 두 유물이 공급하면 **덜 침식되는 쪽**을 쓰기로 했다. 둘 다 끼고 있는데 위험한 쪽으로 흘려보낼 이유가 없다.
  - 기존 시나리오(`mobFight`·`bossFight`)로는 **유물이 바뀌면 무엇이 달라지는가를 잴 수 없었다** — 파티에게 고정 AI 프로필을 쥐여주기 때문이다. 이걸 몰랐다면 "테스트는 통과하는데 아무것도 측정되지 않은" 상태로 끝났다.
  - 실측(보스 Lv20, 시드 300): 폭주 102 / 114 / 228회 (계수 0.8 / 1.0 / 1.4). 비례가 실제로 나온다.
  - 계수 비교는 **계수만 다른 합성 유물**로 했다. 실제 유물끼리 비교하면 스탯 보정과 태그가 함께 달라진다 — ADR-010 에서 원인을 잘못 읽은 이유가 그것이었다.
- 다음에 걸릴 것 같은 것:
  - 스킬 객체가 더 이상 전역 단일 인스턴스가 아니다. 참조 동일성에 기대는 코드가 생기면 조용히 깨진다.
  - T-027 은 `relicFight` 위에 조합 훑기를 얹으면 된다. 실행 시간이 관건이다.

### 2026-08-05 · T-030 · 구역(안전지대)과 위치 표시
- 한 일: `core/world/zone.ts`(사각형 여러 개 = 한 구역, 빈틈·겹침 검사), `data/zones.ts`(입구 6구역·지하 4구역), `data/maps.ts` 에 맵 이름, 좌상단 위치 배너(`맵 · 구역` + `안전`/`조우`), 인카운터 판정을 구역에 연결. 주인공을 도형에서 Tiny Dungeon 캐릭터 타일로 교체. 테스트 447 → 474개
- 알게 된 것:
  - **사용자가 말한 "마을"은 마을 맵이 아니라 NPC 셋이 서 있는 유적 입구 홀이었다.** 지도상 존재하지 않는 것을 사용자가 지목할 때, 그건 이름의 착오가 아니라 **화면이 그렇게 읽힌다는 증거**다. 실제로 그 방은 마을처럼 생겼고 마을처럼 굴어야 했다.
  - 안전지대에서 **걸음을 세지 않기로** 한 것이 판정 자체보다 중요하다. 세기만 하고 발생만 막으면 야영지를 오래 돌아다닌 뒤 한 발짝 나가는 순간 전투가 터진다 — 안전지대가 오히려 함정이 된다.
  - 빈틈 검사(걸어갈 수 있는 모든 칸이 어느 구역엔가 속한다)가 이 시스템의 값을 지킨다. 손으로 계산한 사각형 10개가 첫 실행에 통과했지만, **통과했다는 사실보다 틀렸으면 즉시 알았을 것이라는 점이 중요하다.**
  - 안전지대는 **아무 일도 일어나지 않음**으로만 드러난다. 그래서 스모크가 "40걸음 걸어도 전투 없음" 뒤에 "밖으로 나가면 위험 구역" 까지 확인한다 — 앞만 보면 인카운터가 통째로 꺼져 있어도 초록이다.
- 다음에 걸릴 것 같은 것:
  - 인카운터 테이블은 여전히 맵 단위다(T-032). 구역이 생겼으니 층 대신 구역이 난이도 축이 되는 게 자연스럽다.
  - 맵을 고칠 때 구역을 함께 고치는 것을 잊기 쉽다. 잊으면 테스트가 잡지만, 새 맵을 추가하는 태스크는 그만큼 커진다.

### 2026-08-05 · T-025 · 각인 성장
- 한 일: `core/relic/attunement.ts` — 숙련도 누적, 단계 판정, 다음 단계까지 남은 양. `RelicActive` 에 `unlockRank` 추가해 잠긴 스킬 도입. 전투에서 쓴 스킬이 유물 숙련도로 환산된다. 테스트 425 → 447개
- 알게 된 것:
  - **`gainAttunement` 의 인자가 액터가 아니라 유물 id 다.** 시그니처 자체가 GDD §5.3("숙련도는 유물에 귀속")을 강제한다. 착용자를 바꿔도 유지되는 것을 테스트로 고정했다 — 이게 "계승" 이라는 제목의 의미이자, 후반에 새 파티원이 합류해도 기존 자산이 죽지 않게 하는 장치다.
  - **`activesOf` 가 숙련도를 모르면 0단계로 본다.** 기본값을 "전부 해금" 으로 두면 시뮬레이터나 테스트가 잠긴 스킬을 실수로 쓰게 된다. 모를 때는 가장 보수적인 값이 안전하다.
  - **0단계에 쓸 수 있는 액티브가 없는 유물을 검증에서 거부한다.** 끼워도 아무 일이 일어나지 않는 유물이 된다.
  - `sundering-arc` 를 `sundering-core` 의 2단계 해금으로 옮겼다. **유물을 계속 쓸 이유가 여기서 나온다** — 처음엔 약한 스킬만 주고, 쓸수록 진짜 위력이 열린다.
  - 스킬 id 를 유물로 되짚어야 숙련도를 어디에 쌓을지 알 수 있다. 같은 스킬을 두 유물이 공급하면 장착한 쪽에 쌓인다.
- 다음: T-026 유물별 침식 계수 연동. 여기서 공명의 효과 종류도 함께 넓힌다(T-024 에서 미뤄둔 것).

### 2026-08-05 · T-024 · 공명
- 한 일: `core/relic/resonance.ts` — 태그 세기, 조건 판정, 보정 합산, 부족분 안내. 표본 공명 3종. `partyStore` 가 파티 전원에게 공명 보정을 적용. 테스트 401 → 425개
- 알게 된 것:
  - **"유물 하나가 조합 공간을 곱으로 늘린다" 는 말의 실체가 여기다.** 유물을 추가하면 그 유물이 들어가는 조합만 늘어나는 게 아니라 **성립 가능한 공명의 수가 함께** 늘어난다. 이 성질을 테스트로 고정했다 — 유물을 더해서 발동 공명이 줄어드는 일은 없어야 한다.
  - **한 유물이 혼자 공명을 발동시킬 수 있다.** `가르는 핵` 은 storm·hollow 를 동시에 가져 혼자서 `무너지는 울림` 을 켠다. 버그가 아니라 그 유물의 값어치이므로 테스트에 의도를 적어뒀다.
  - **조건 없는 공명을 거부한다.** 항상 발동하는 건 공명이 아니라 그냥 전역 보정이고, 조합 설계에 아무것도 기여하지 않는다.
  - **시작 유물만으로 공명 하나는 발동하도록** 콘텐츠 규약을 걸었다. 하나도 안 켜지면 플레이어가 공명이라는 개념 자체를 만나지 못한다.
  - 효과 종류를 스탯 보정 하나로 시작했다 — T-022 에서 패시브를 뺀 것과 같은 판단이다. 쓰이지 않는 효과 종류를 미리 만들면 죽은 구조가 된다.
- 다음: T-025 각인 성장. 숙련도가 **유물에 귀속**되어 착용자를 바꿔도 유지되는 것이 GDD §5.3 의 요점이다.

### 2026-08-05 · T-023 · 유물이 능력의 출처가 된다 (ADR-004 완성)
- 한 일: `partyStore` 를 로드아웃 기반으로 재구성. 스킬은 장착 유물에서, 스탯은 기본값 + 유물 보정으로 매번 계산. `encounters.ts` 의 하드코딩된 `partySkills` 제거. 테스트 392 → 401개
- 알게 된 것:
  - **보정된 스탯을 저장하면 안 된다.** 전투를 치를 때마다 유물 보정이 겹쳐 쌓인다. 저장하는 것은 "지금 얼마나 다쳤는가"(HP·MP·침식·상태이상)뿐이고, 스탯은 기본값에서 매번 다시 계산한다. 이걸 테스트로 고정했다 — 두 번 전투해도 스탯이 같아야 한다.
  - **유물을 빼면 최대치가 줄어들 수 있으므로 현재 값을 잘라야 한다.** MP 최대치가 +3 인 유물을 빼면 현재 MP 가 최대치를 넘게 된다.
  - **기본 배분을 한 사람에게 몰아주지 않고 한 명씩 돌아가며 준다.** 처음엔 앞에서부터 채웠는데 두 유물이 모두 vanguard 에게 갔다. 유물이 능력의 출처라서 몰아주면 **나머지가 기본 공격만 하는 허수아비**가 된다.
  - 유물을 끼우지 않은 파티는 기본 공격만 할 수 있다. `partySkills` 가 빈 목록이 되는 것이 정상 동작이다 — 예외를 던지지 않는다.
- 다음: T-024 공명. 여기서 "조합이 곧 전략" 이라는 GDD §5.2 가 실제로 성립하기 시작한다.

### 2026-08-05 · T-022 · 유물 자료 구조와 장착 (M3 시작)
- 한 일: `core/relic/index.ts` — `Relic` 타입, 8슬롯 로드아웃, 장착/해제, 스탯 합산, 액티브 수집, 스키마 검증. 표본 유물 3종. 테스트 360 → 392개
- 알게 된 것:
  - **같은 유물을 둘이 나눠 낄 수 없게 막았다.** 허용하면 유물 하나로 조합 공간을 부풀릴 수 있고, 그러면 "좋은 유물 하나를 전원에게" 가 정답이 되어 조합 설계가 무의미해진다. 단, 이미 끼운 슬롯에 같은 유물을 다시 끼우는 것(교체 취소)은 허용한다.
  - **스탯 보정이 음수로 내려가지 않게 막았다.** 대가가 큰 유물이 방어력을 깎을 수 있는데, 음수가 되면 데미지 공식(ADR-009)의 감산 항이 오히려 피해를 늘린다.
  - **태그·액티브·기록이 없는 유물을 검증에서 거부한다.** 태그가 없으면 어떤 공명에도 기여하지 못하고, 액티브가 없으면 ADR-004("능력의 출처는 유물")를 배신하며, 기록이 없으면 서사 전달 수단이 사라진다. 셋 다 "있어도 되는 필드"가 아니라 유물의 정의다.
  - **등급이 높을수록 침식 계수가 크다**는 규약을 테스트로 고정했다. 스킬에서 했던 것과 같은 이유다 — 이 비례가 무너지면 "가장 센 것만" 이 정답이 된다.
- 범위 조정: **패시브를 넣지 않았다.** GDD §5.1 에 있지만 효과를 소비할 기제가 아직 없어 죽은 구조가 된다. T-024·T-026 에서 실제 효과와 함께 정의한다.
- 다음에 걸릴 것 같은 것:
  - 표본 유물이 3종뿐이라 조합 검증이 거의 자명하다. T-027 불변식이 의미를 가지려면 어느 정도 종류가 필요한데, 규칙 9 는 그 반대 순서를 요구한다 — **T-027 에서 "표본 유물을 프로그램으로 생성해 조합 공간을 만드는" 방식이 필요할 수 있다.**

### 2026-08-05 · 밸런스 · 레벨 간 난이도 붕괴 수정 (ADR-010)
- 한 일: 침식 임계를 최대 MP 비례로, 보스 MP 배수 2→1, `sundering-arc` 비용 9→14·침식 34→40. 불변식 8 추가. 테스트 354 → 360개
- **결과: 보스전 승률 99/44/39 → 100/98/63** (Lv5/20/40)
- 알게 된 것 — **T-019 의 진단이 틀렸다**:
  - 당시 "침식 임계가 고정이라 후반에 폭주가 누적된다"고 보고했다. 사용자가 그 진단을 근거로 (a)를 골랐는데, **적용하니 승률이 오히려 더 떨어졌다** (Lv20 44%→32%, Lv40 39%→23%).
  - 변수를 분리해 재보니 폭주하던 쪽은 주로 **보스**였다. 고정 임계가 고레벨 보스에 대한 **의도치 않은 제동장치**로 작동하고 있었고, 임계를 비례로 바꾸자 그 제동이 풀렸다.
  - 진짜 원인은 **보스가 액터 하나라 MP 가 곧 고위력 스킬의 시전 횟수**라는 것이었다. MP 배수 2 + 위력 220 스킬의 비용 9 = 레벨이 오를수록 실효 화력이 초선형 증가.
  - 침식 비례 자체는 유지했다. "MP 를 얼마나 쓰면 폭주하는가" 가 레벨과 무관해지는 건 그 자체로 옳은 설계이고, **폭주를 밸런스 제동장치로 쓰는 것은 우연에 기대는 일**이다.
- 교훈: 시뮬레이터는 "무엇이 잘못됐는지" 를 정확히 보여줬지만 **"왜" 는 사람이 잘못 읽었다.** 원인을 지목할 때는 변수를 분리해 재보고 말해야 한다. 밸런스 가설은 실측 전까지 가설일 뿐이다.
- 남긴 것: 불변식 8(레벨 간 승률 차이 45%p 이내)로 회귀를 막는다. 현재 측정치는 37%p 로 통과하되 여유가 크지 않다 — 이 축을 건드리면 바로 드러난다.

### 2026-08-05 · T-021 · 인카운터 연결 (M2 완료)
- 한 일: `core/world/encounter.ts` (걸음 수 카운터, 가중치 추첨), 지역별 인카운터 테이블, `game/partyStore.ts` (전투 밖으로 이어지는 파티 상태), 탐색 ↔ 전투 전환과 복귀. 테스트 339 → 354개
- 알게 된 것:
  - **매 걸음 확률을 굴리지 않고 걸음 수 카운터를 쓴다.** 확률 방식은 운이 나쁘면 두 걸음 만에 두 번 싸우고 운이 좋으면 한 층을 그냥 지나간다. "최소 8걸음은 안전, 최대 20걸음 안에 반드시 한 번" 이 탐색의 리듬을 예측 가능하게 만든다. 두 성질을 각각 테스트로 고정했다.
  - **계단이 인카운터보다 먼저다.** 층을 옮기는 걸음에서 전투가 터지면 어느 층에서 싸우는지 모호해진다.
  - **파티 상태를 씬 밖에 뒀다.** 씬은 오갈 때마다 새로 만들어지므로 HP·MP·침식이 사라진다. 세이브(M4) 전까지는 메모리에 둔다 — 새로고침하면 초기화되는데, 그건 세이브가 할 일이지 이 모듈이 할 일이 아니다.
  - **월드 난수도 시드 고정이다** (ADR-002). "몇 걸음째에 뭐가 나왔는지" 를 재현할 수 있다.
- 걸렸던 것:
  - **인카운터가 생기자 기존 탐색 스모크가 깨졌다.** 16걸음을 걷는 동안 전투가 끼어들었다(임계 8~20). `?encounters=off` 개발 플래그를 추가해 분리했다 — 맵을 손보는 동안 5걸음마다 전투가 터지면 확인이 불가능하므로 어차피 필요한 기능이다.
  - 스모크에서 복귀 위치를 한 걸음 어긋나게 잡았다. 전투는 **이동을 마친 뒤** 벌어지므로 복귀 지점은 마지막으로 밟은 칸이다.

### 2026-08-05 · T-020 · 전투 씬 UI (M2 에서 처음으로 화면에 나타남)
- 한 일: `game/scenes/BattleScene.ts` — 프론트뷰, HP·MP·침식 게이지, 커맨드/스킬/아이템 메뉴, 대상 커서, 이벤트 로그 재생, 데미지 팝업. `data/encounters.ts` 편성. 스모크가 전투 1회를 끝까지 진행
- 알게 된 것:
  - **이벤트 로그가 애니메이션 스크립트로 충분했다.** T-013 이후 "T-020 에서 판명된다"고 미뤄둔 질문의 답이다. `damage` 에 넣어둔 `critical`·`elementMod` 가 그대로 연출 분기("치명타!", "약점!", "저항…")가 됐고, 씬은 판정을 하나도 하지 않는다.
  - **씬이 `data-battle-phase` 를 노출하지 않으면 스모크를 쓸 수 없다.** 연출이 도는 동안 키를 넣으면 씹히므로, 테스트가 "지금 입력을 받는 중인지" 를 알아야 한다. 씬 상태를 DOM 으로 내보내는 패턴(T-006 의 `data-scene`)이 여기서 다시 값을 했다.
  - **쓰러진 적의 이름표가 남는 문제**를 스크린샷으로 발견했다. 스프라이트만 감추고 라벨을 잊었는데, 테스트는 통과했다 — 눈으로 보지 않으면 안 잡히는 종류다.
  - 전투 진입은 `?scene=battle` 개발용 진입점을 썼다. 탐색 → 전투 경로는 T-021 이 만든다.
- 걸렸던 것:
  - 스모크가 한 번 `ERR_CONNECTION_REFUSED` 로 실패했다. 좀비 프로세스는 없었고 TIME_WAIT 소켓만 남아 있었다 — 직전 실행의 dev 서버 종료와 새 실행의 포트 바인딩(`strictPort: true`)이 겹친 경합이다. 재실행하면 통과한다. **연속 실행 시 드물게 나올 수 있는 플레이크**로 기록해 둔다.

### 2026-08-05 · T-016b · 아이템 커맨드
- 한 일: `core/battle/item.ts` — 인벤토리, 회복·해독·정화·부활. `BattleState` 에 소지품 추가. 아이템 5종. 테스트 314 → 339개
- 알게 된 것:
  - **아이템은 "자원을 소비해 규칙을 되돌리는 수단"이다.** 피해·상태이상·침식·죽음 네 축을 각각 되돌리도록 다섯 개를 짰다. 하나라도 빠지면 그 축은 되돌릴 수 없는 축이 되고 전투 설계의 자유도가 줄어든다. 네 종류가 모두 있는지, 모든 상태이상에 치료 수단이 있는지를 테스트로 고정했다.
  - **헛되이 소모되는 사용을 막는다.** 온전한 대상에게 회복, 침식 없는 대상에게 정화석, 걸린 게 없는데 해독 — 전부 거부한다. 아이템이 사라졌는데 아무 일도 안 일어나면 버그처럼 느껴진다.
  - **부활 아이템만 쓰러진 대상을 노린다.** `livingTarget` 을 쓰지 않고 따로 검증한다. T-014 에서 "회복은 부활시키지 못한다" 고 정한 규칙이 여기서 값을 한다 — 부활을 별도 아이템으로 만들 이유가 생겼다.
  - 부활 비율을 0.5 미만으로 못 박았다(0.35). 부활이 값싸면 전멸의 긴장이 사라진다.
  - 실제 회복량·정화량을 결과로 함께 돌려준다. 최대치에 막혀 40 중 20만 들어갔다면 이벤트도 20이어야 한다.
- 걸렸던 것:
  - 테스트에서 턴 순서를 잘못 가정했다(민첩 순은 hero → ally → slime인데 slime 을 먼저 넣었다). `step()` 이 "지금 차례는 ally 인데 slime 의 커맨드" 라고 정확히 잡아줬다 — T-013 에서 넣은 방어가 실제로 작동한 사례다.

### 2026-08-05 · T-019 · 밸런스 시뮬레이터와 불변식 (M2 분기점)
- 한 일: `core/battle/simulate.ts`, `tools/sim.ts` (`npm run sim`), `tests/balance/`, 성장 곡선·표준 시나리오 데이터. 테스트 301 → 314개 + todo 4개
- **측정 결과 (시드 300, 균형 편성)**

  | 시나리오 | 승률 | 라운드 | 남은HP | 폭주 |
  |---|---:|---:|---:|---:|
  | 잡몹 Lv5 | 100% | 4.6 | 87% | 0 |
  | 잡몹 Lv20 | 100% | 5.1 | 86% | 0 |
  | 잡몹 Lv40 | 100% | 5.2 | 86% | 0 |
  | 보스 Lv5 | 99% | 15.9 | 30% | 147 |
  | 보스 Lv20 | **42%** | 19.1 | 6% | 676 |
  | 보스 Lv40 | **37%** | 19.5 | 6% | 724 |

- 알게 된 것:
  - **전투 길이는 목표 안이다.** 잡몹 4.6~5.2 라운드(목표 3~6), 보스 15.9~19.5(목표 12~25). 타임아웃 0. ADR-009 의 공식 선택이 틀리지 않았다는 첫 실측 증거다.
  - **보스전 승률이 레벨에 따라 급락한다 (99% → 37%).** 원인은 침식 임계가 고정값이라 전투가 길어지는 후반에 폭주가 누적되는 것 — 폭주 횟수가 147 → 724 로 5배 늘었다. **사람의 결정이 필요해 백로그에 올렸다.**
  - **잡몹전 승률이 100%라 변별력이 없다.** 단판만 재고 있어서다. 진짜 압박은 연속 전투(유적 완주)에서 오므로 M4 이후에 다시 재야 한다.
  - **불변식 7개 중 3개는 유물이 없어 측정 불가**였다. `it.todo` 로 남겨 M3 에서 실제 테스트로 바꾼다. CLAUDE.md 규칙 9(시뮬레이터 없이 유물 데이터를 늘리지 않는다)와 짝을 이룬다.
  - AI 용 난수를 전투용과 분리했다. 같은 스트림을 쓰면 AI 선택이 전투의 변동폭을 밀어내 같은 시드에서도 전개가 달라진다.
  - 시뮬레이터는 판정하지 않고 숫자만 낸다. 합격·불합격은 `tests/balance/` 가 정한다 — 도구와 기준을 섞으면 기준을 조용히 낮추기 쉬워진다.
- 남긴 것:
  - `src/data/progression.ts` 의 성장 곡선은 **잠정값**이다. GDD 에 곡선이 없어 ADR-009 검증 때 쓴 선형을 옮겼다. 곡선을 바꾸면 위 측정치도 바뀐다.
  - `vite-node` 를 devDependency 로 추가했다 (CLI 실행용, 런타임 의존성 아님).

### 2026-08-05 · T-018 · 적 AI
- 한 일: `core/battle/ai.ts` — 가중치 행동 선택, HP 임계 페이즈, 대상 선택 3종(random/weakest/toughest). 표본 프로필 2종. 테스트 280 → 301개
- 알게 된 것:
  - **AI 를 상태가 아니라 정책으로 뒀다.** `BattleState` 에 넣지 않고 밖에서 커맨드를 만들어 넣는다. 덕분에 상태 머신은 AI 를 모르고, 시뮬레이터(T-019)는 같은 전투에 다른 AI 를 붙여 실험할 수 있다.
  - **쓸 수 없는 선택지를 미리 배제한다.** MP 부족·침묵·폭주 상태에서 스킬을 고르면 `step()` 이 던진다. AI 가 규칙을 어겨 전투를 멈추는 것보다 애초에 고를 수 없게 하는 편이 낫다. 남는 게 없으면 기본 공격으로 떨어진다.
  - **대상이 없으면 던지지 않고 방어한다.** 전투가 끝나가는 프레임에서 AI 가 예외를 던지면 원인 추적이 어렵다.
  - **페이즈 목록 순서가 곧 우선순위**라 좁은 조건을 먼저 둬야 한다. 넓은 조건이 위에 있으면 좁은 조건은 영원히 도달하지 않는다 — 이 함정을 테스트로 박제했다.
  - 대상 선택의 동점도 id 순으로 끊는다. 턴 순서와 같은 이유로, 정렬이 불안정하면 재현이 깨진다.
  - 콘텐츠 규약 테스트를 붙였다: 모든 프로필의 마지막 페이즈는 조건이 없어야 한다. 없으면 특정 HP 구간에서 갈 페이즈가 사라진다.
- 다음: **T-019 시뮬레이터.** 여기서 루프가 자동으로 멈춘다. 지금까지 "T-019 가 조정할 대상"으로 미뤄둔 추정값(침식 임계 100, 마비 40%, 혼란 50%, 방어 배율 0.5, 도망 0.5, 치명타 계수)이 전부 처음 검증된다.

### 2026-08-05 · T-017 · 상태이상 5종
- 한 일: `core/battle/status.ts` — 독·마비·수면·침묵·혼란. `step()` 에 행동 불가·대상 교란·턴 종료 처리. 스킬에 `inflict` 추가. 테스트 251 → 280개
- 알게 된 것:
  - **다섯 가지가 서로 다른 방식으로 커맨드 선택을 제약하게 만들었다.** 피해만 주는 이상은 그냥 약한 공격이지 상태이상이 아니다. 독만 제약이 없고, 나머지는 행동 자체(수면·마비), 선택지(침묵), 대상(혼란)을 건드린다.
  - **적용 우선순위를 정하고 GDD에 명문화했다** — 폭주 → 수면·마비 → 혼란 → 침묵. 수면(확정)을 마비(확률)보다 먼저 보는 이유가 미묘한데, 순서를 뒤집으면 잠든 액터가 마비 판정에 난수를 소비해 **같은 시드에서 다른 전개가 나온다.**
  - **같은 이상 중복 시 누적하지 않고 긴 쪽으로 갱신한다.** 누적하면 반복 시전으로 영구 봉인이 되고 "수면 걸고 때리기"가 지배 전략이 된다.
  - **수면은 피격하면 깨어난다.** 자는 상대를 계속 때려 굳히는 전략을 막는다. `strike` 안에 넣어 공격·스킬·폭주가 모두 같은 규칙을 따르게 했다.
  - **독 피해를 지속 턴 감소보다 먼저 처리한다.** 순서를 뒤집으면 마지막 턴에 독이 한 번 덜 들어간다.
  - 쓰러진 대상에게는 상태이상을 걸지 않는다. 시체에 독을 거는 이벤트가 로그에 남으면 연출이 이상해진다.
- 다음에 걸릴 것 같은 것:
  - 마비 40%, 혼란 50% 는 "가끔 억울하지만 대책은 세울 수 있는" 정도를 노린 추정이다. T-019 시뮬레이터가 조정 대상.
  - 상태이상 해제 수단이 아직 없다(아이템은 T-016b, 정화는 M4). 지금은 지속 턴이 끝나기를 기다리는 수밖에 없다.

### 2026-08-05 · T-016 · 스킬과 침식
- 한 일: `core/battle/skill.ts` — MP 소비, 침식 누적, 폭주. `step()` 에 스킬 커맨드와 폭주 처리. 표본 스킬 3종. 테스트 229 → 251개
- 범위 조정: **아이템을 T-016b 로 분리했다.** 인벤토리(수량 관리)가 전투 상태에 들어와야 해서 한 이터레이션에 넣기엔 컸다
- 알게 된 것:
  - **폭주 판정을 커맨드 검증보다 먼저 한다.** 제어를 잃은 상태에서 무엇을 골랐든 의미가 없다. 이 순서를 뒤집으면 "폭주 중인데 MP 부족 에러"처럼 엉뚱한 메시지가 나온다.
  - **폭주 후 침식이 절반만 해소된다.** 완전히 씻기면 "일부러 폭주시키고 다시 시작"이 최적 전략이 되어 리스크 축이 무너진다. 거점 정화소에 갈 이유를 남겨야 한다.
  - **스킬 위력과 침식의 비례를 테스트로 고정했다.** 위력 순으로 정렬했을 때 침식도 단조증가해야 한다. 이 비례가 깨지면 "가장 센 것만 계속 쓴다"가 정답이 되어 조합 설계가 무의미해진다. 앞으로 스킬을 추가할 때 이 테스트가 지켜준다.
  - **커맨드가 스킬 id 가 아니라 스킬 객체를 담는다.** 상태 머신이 콘텐츠 레지스트리를 알 필요가 없어지고, 시뮬레이터가 가상의 스킬을 주입해 실험하기도 쉬워진다.
  - `skillBlockReason` 은 불리언이 아니라 이유 문자열을 돌려준다. UI 가 "왜 회색인지" 를 보여줘야 하고 AI 도 같은 판단을 재사용한다.
- 다음에 걸릴 것 같은 것:
  - 침식 임계 100, 해소 비율 0.5 는 표본 스킬 기준 추정이다. T-019 시뮬레이터가 실제 전투 길이에 맞춰 조정한다.
  - 폭주가 아군을 때릴 수 있는데, 파티가 1명이면 때릴 대상이 없어 아무 일도 안 일어난다. 지금은 정상 동작이지만 연출 단계에서 어색할 수 있다.

### 2026-08-05 · T-015 · 기본 커맨드 (공격·방어·도망)
- 한 일: `step()` 이 처음으로 실제 효과를 처리한다. 공격→피해→사망→승패, 방어 상태와 배율, AGI 기반 도망. `BattleOutcome` 에 `fled` 추가, `validTargets`·`fleeChance` 공개. 테스트 214 → 229개
- 알게 된 것:
  - **방어 배율은 관통 하한 뒤에 곱한다.** 하한은 "방어력으로 막을 수 없는 몫"이지 "방어 커맨드로도 못 줄이는 몫"이 아니다. 앞에 곱하면 방어를 골라도 하한에서 멈춰 한 턴을 버린 대가가 사라진다. 테스트로 고정했다 (DEF 10,000 상대에게 8 → 방어 시 4).
  - **도망 성공률 상한을 95%로 뒀다.** 100%면 모든 전투가 선택 사항이 되고 유물 조합을 고민할 이유가 사라진다.
  - **적의 도망은 지원하지 않는다.** 몬스터가 달아나는 연출이 필요해지면 별도 커맨드로 만든다 — 지금 넣으면 `fled` 결과의 의미가 파티 기준인지 액터 기준인지 모호해진다.
  - `step()` 이 이제 RNG 를 소비하므로 상태 저장 지점을 한 곳으로 모았다. 커맨드 효과와 턴 순서 재생성이 같은 스트림을 쓴다 — 순서가 섞이면 재현이 깨지므로 한 번 복원해 한 번 저장한다.
- 다음에 걸릴 것 같은 것:
  - 방어 배율 0.5, 도망 기본 확률 0.5 는 근거 없는 초기 추정이다. T-019 시뮬레이터가 조정할 대상.
  - 이벤트 로그가 애니메이션에 충분한지는 아직 모른다. `damage` 에 critical·elementMod 를 넣어 연출 분기 근거는 마련했지만, 실제 판정은 T-020 에서 난다.

### 2026-08-05 · T-014 · 데미지 해석
- 한 일: `core/battle/damage.ts` — ADR-009 공식, 속성 내성, 치명타, `applyDamage`/`applyHeal`. `data/battle.ts` 에 상수 분리. `BattleEvent` 에 damage/heal/death 추가. 테스트 193 → 214개
- 알게 된 것:
  - **관통 하한이 실제로 무적을 막는지 테스트로 고정했다.** ATK 42 vs DEF 200 에서 순수 감산형은 1(무적)이 되지만 관통 하한으로는 8이 나온다. 테스트가 두 값을 나란히 계산해 비교하므로, 나중에 누가 하한을 없애면 즉시 드러난다.
  - **속성 상성을 5x5 고정표가 아니라 액터별 내성으로 뒀다.** 고정표는 "불은 흙에 강하다"를 전역으로 못박는데, 이 게임의 재미는 적마다 다른 약점을 찾아 유물 조합을 맞추는 데 있다. 적이 스스로 약점을 선언하는 편이 콘텐츠 자유도가 높다.
  - **RNG 소비 순서를 변동폭 → 치명타로 고정했다.** 순서가 바뀌면 같은 시드로도 다른 결과가 나온다. 주석에 이유를 적어뒀다 — 나중에 누가 무심코 순서를 바꿀 수 있는 지점이다.
  - **회복은 쓰러진 액터를 일으키지 못한다.** 회복 아이템이 조용히 부활시키면 부활의 의미가 사라진다. 이런 판정은 코드만 봐서는 의도인지 버그인지 알 수 없어 테스트 이름에 이유를 적었다.
- 다음에 걸릴 것 같은 것:
  - 치명타 확률 상한 50%, 행운 계수 0.002 는 근거 없는 초기 추정이다. T-019 시뮬레이터가 조정할 대상.
  - `damage`/`heal`/`death` 이벤트 타입만 추가했고 아직 아무도 내보내지 않는다. T-015 커맨드에서 연결된다.

### 2026-08-05 · T-013 · 전투 상태 머신 골격 (M2 시작)
- 한 일: `core/battle` — `BattleState`/`Command`/`BattleEvent`, 순수 `step()`, AGI 기반 턴 큐, 승패 판정. `data/battle.ts` 에 튜닝값 분리. 테스트 171 → 193개
- 알게 된 것:
  - **동점을 id 순으로 끊는다.** 정렬이 불안정하면 같은 시드로도 다른 전개가 나와 재현이 깨진다. 결정론은 정렬 하나로 무너질 수 있다.
  - **턴 순서 흔들림(15%)은 인접한 민첩끼리만 뒤바꾼다.** 흔들림이 없으면 순서가 영원히 고정돼 기계적으로 느껴지고, 크면 민첩에 투자할 이유가 사라진다. 테스트로 "민첩 30과 10은 어떤 시드에서도 안 뒤집힌다"를 고정했다.
  - **양쪽 동시 전멸은 패배로 본다.** 살아남지 못했다면 이긴 것이 아니다. 이런 판정은 나중에 슬쩍 바뀌기 쉬워서 테스트에 의도를 적어뒀다.
  - 차례가 아닌 액터의 커맨드는 던진다. UI 와 상태가 어긋난 채 진행하면 엉뚱한 액터가 행동하고 원인 추적이 어려워진다.
- 다음에 걸릴 것 같은 것:
  - `step()` 이 아직 커맨드 효과를 처리하지 않는다(턴만 넘긴다). T-014 데미지, T-015 커맨드에서 채운다.
  - 이벤트 로그가 애니메이션 스크립트가 될 예정인데(T-020), 지금은 `turnStart`/`turnEnd` 뿐이라 그 구조가 충분한지는 데미지 이벤트가 붙어야 드러난다.

### 2026-08-05 · T-012 · 데이터 검증 커널 (M1 완료)
- 한 일: `core/validation` (문제 수집기·경로 접두사·읽기 도우미·중복 검사기), `core/data/registry` (키↔id 일치, 교차 참조), 대화·포탈 검증기를 커널 위로 이관. 테스트 157 → 171개
- 알게 된 것:
  - **Zod를 도입하지 않기로 했다 (ADR-008).** 백로그가 Zod를 전제했지만, 그 사이 생긴 검증기들이 실제로 잡는 것의 상당수는 **Zod가 표현할 수 없는 교차 규칙**이었다 — 어떤 타일셋에도 속하지 않는 gid, collision 레이어 누락, 도착 지점이 반대편 포탈 위. Zod를 넣어도 이 규칙들은 그대로 손으로 써야 하고 얻는 건 형태 검사뿐이다. 대신 네 검증기가 공유하던 뼈대를 뽑았다.
  - 결과적으로 **규칙 8(런타임 의존성 승인)에 걸리지 않고 진행**했다. 규칙을 우회한 게 아니라, 규칙 덕분에 "정말 필요한가"를 한 번 더 물은 셈이다.
  - **T-012의 범위를 조정했다.** 원래 DoD는 유물·스킬·아이템·적 스키마 정의였는데, 소비하는 시스템 없이 스키마부터 정하면 틀리게 설계될 가능성이 높다. 그 부분은 M2·M3으로 옮기고, 지금 필요한 "스키마를 싸게 추가할 토대"를 만들었다.
  - `Problems.scope()` 로 `entries[2].source: ...` 같은 위치를 자동으로 붙인다. 손으로 접두사를 이어붙이던 코드가 사라졌다.
  - 에러 메시지에서 주어 뒤 조사를 뺐다. `${subject}이(가)` 형태는 받침 유무에 따라 "포탈 배치이(가)" 처럼 어색해진다.
- 남긴 것:
  - 에셋 색인·타일맵 검증기는 아직 커널로 옮기지 않았다. 메시지에 위치 접두사가 손으로 박혀 있어 옮기면 문자열이 바뀌고 테스트가 깨진다. 다음에 그 파일을 손댈 때 함께 옮긴다.
- 다음: **M1 완료.** 다음은 M2 전투 코어 — 성격이 크게 달라진다. 스크린샷으로 확인할 것이 줄고 순수 로직과 시뮬레이터가 중심이 된다.

### 2026-08-05 · T-011 · 맵 간 이동
- 한 일: `core/world/portal.ts`, 지하 1층 맵(40x30) 추가, `game/world/mapRegistry.ts` (맵 로딩·캐시), 씬 재시작 기반 층 이동. 테스트 137 → 157개
- 알게 된 것:
  - **가장 위험한 실수는 도착 지점이 반대편 포탈 위에 있는 것이다.** 밟자마자 다시 발동해 두 맵을 무한히 오가고 플레이어는 조작 불능이 된다. 런타임에 발견하면 원인을 짚기 어려워서 `validatePortalNetwork` 가 배치 단계에서 막는다.
  - 한 방향만 뚫린 계단(돌아올 길 없음)도 콘텐츠 테스트로 막았다. 내려갔다가 갇히는 건 세이브가 없는 지금 특히 치명적이다.
  - **맵 목록(`data/maps.ts`)과 맵 로딩(`game/world/mapRegistry.ts`)을 분리했다.** `?raw` import 는 Vite 전용이라 게임 레이어에만 둘 수 있는데, 유닛 테스트는 맵 파일을 읽어야 한다. 경로만 데이터로 두고 테스트는 디스크에서 직접 읽는다.
  - 포탈은 이동 트윈이 **끝난 뒤에** 확인한다. 걷는 도중에 화면이 바뀌면 어색하다.
  - 씬 재시작 후 몇 프레임 동안 입력이 살아 있어 `leaving` 플래그로 막았다.
- 사고 기록:
  - **PowerShell 로 한글이 든 파일을 텍스트 치환했다가 인코딩이 깨지고 줄바꿈이 먹혔다.** 방 하나가 주석 뒤로 밀려 맵에서 사라졌는데, 스모크 경로가 그 방을 지나지 않아 테스트는 통과했다. 스크린샷을 눈으로 보고서야 발견했다. **한글 파일 편집에 PowerShell 치환을 쓰지 않는다.**
  - 타일 인덱스를 두 번 잘못 골랐다(13, 1 모두 장식 무늬가 있는 변형). 7배 확대 시트로도 미세한 무늬는 판별되지 않는다. **타일 선택은 실제로 깔아보고 스크린샷으로 확인해야 한다.**
- 다음에 걸릴 것 같은 것:
  - 층을 오갈 때 씬을 통째로 다시 만든다. 맵이 커지면 RenderTexture 재생성 비용이 드러날 수 있다.
  - 포탈이 도형 표식이다. 계단 타일을 백로그에 사람 몫으로 올렸다.
  - 다음은 T-012 데이터 스키마(Zod). M1 의 마지막이고, 여기서 런타임 의존성 추가를 정식 제안한다.

### 2026-08-05 · T-010 · 대화 시스템
- 한 일: `core/dialogue` (줄바꿈·쪽 나눔·진행 상태), `core/world/interaction` (마주 본 대상 찾기, NPC 충돌), `game/ui/TextBox`, 유적 입구 NPC 3명과 대사. 테스트 99 → 137개
- 알게 된 것:
  - **콘텐츠 정합성 테스트를 처음 만들었다** (`tests/unit/content.test.ts`). 벽 속에 선 NPC, 없는 대화 ID 참조, 스폰 칸을 막는 NPC, 상자 밖으로 넘치는 대사를 잡는다. 루프가 콘텐츠를 늘릴수록 이런 실수가 늘어나므로 지금 세워두는 게 맞다.
  - **`TEXT_BOX_LAYOUT` 을 core 에 뒀다.** 처음엔 UI 상수라 생각해 `game/ui/TextBox.ts` 에 뒀는데, 콘텐츠 테스트가 이 값으로 대사 길이를 검사하려다 Phaser 를 끌어와 `window is not defined` 로 깨졌다. **줄바꿈을 계산하는 주체가 core 이므로 그 파라미터도 core 에 있어야 한다.** ADR-001 이 설계 취향이 아니라 실제로 물린다는 걸 확인한 사례.
  - **NPC 충돌을 지형 충돌과 분리했다.** `stepWalker` 에 선택적 판정식을 넘기는 방식이다. 한 곳에 합치면 NPC 가 움직일 때마다 맵 데이터를 건드려야 한다.
  - **이동은 누르고 있으면 계속, 상호작용은 누른 순간에만.** 상호작용까지 폴링하면 키 한 번에 대화가 끝까지 넘어간다. `JustDown` 을 쓴다.
  - 한국어는 띄어쓰기가 드물어 **긴 단어 하드 브레이크가 기본 경로**가 된다. 단어 경계 접기만 구현했다면 문장이 통째로 상자 밖으로 나갔을 것이다.
- 다음에 걸릴 것 같은 것:
  - NPC 배치를 Tiled 오브젝트 레이어가 아니라 `src/data/npcs.ts` 에 뒀다. 맵이 여러 장으로 늘어나면 재검토가 필요하다.
  - 대화에 분기·플래그가 없다. 퀘스트 단계에서 필요해진다.
  - 다음은 T-011 맵 간 이동. 맵이 둘 이상이 되면서 `MAP_ID` 하드코딩이 깨진다.

### 2026-08-05 · T-009 · 카메라 추적과 경계 클램프
- 한 일: `core/world/camera.ts` (순수 클램프 계산), OverworldScene 카메라 연결, 맵을 30x16 → **60x40(960x640px)** 으로 확장. 테스트 89 → 99개
- 알게 된 것:
  - **맵이 화면에 다 들어오면 카메라는 검증할 수 없다.** 기존 맵(480x256)은 화면(480x270)보다 작아서 카메라를 붙여도 아무 일이 일어나지 않았다. 그래서 맵 확장이 이 태스크의 일부가 됐다 — 다섯 개의 방과 통로로 재구성했다.
  - Phaser 에도 `setBounds` + `startFollow` 가 있지만 규칙을 core 에 뒀다. 맵이 화면보다 작을 때의 처리 같은 경계 조건은 엔진 버전에 따라 달라질 수 있고, 그런 걸 브라우저 띄워 눈으로 확인하는 건 자율 루프가 가장 못하는 일이다.
  - **축마다 독립적으로 클램프한다.** 가로로만 긴 맵에서 세로만 고정되는 게 자연스럽다. 스모크가 이 성질을 직접 확인한다 — 오른쪽으로 이동해 카메라 x 는 24가 되지만 y 는 위쪽 경계에 걸려 0 그대로다.
  - 카메라 갱신은 입력 잠금과 **무관하게** 매 프레임 돈다. 이동 트윈이 도는 동안에도 따라가야 하기 때문이다.
  - 맵 크기를 숫자로 못 박은 테스트가 깨졌다. 크기 자체가 아니라 **"화면에 다 안 들어온다"는 성질**을 검사하도록 바꿨다. 콘텐츠가 자랄 때마다 테스트를 고치게 만들면 안 된다.
- 다음에 걸릴 것 같은 것:
  - 맵이 커지면서 RenderTexture 가 960x640 이 됐다. 지금은 문제없지만 월드맵 규모로 가면 청크 단위 렌더링을 검토해야 한다.
  - 다음은 T-010 대화 시스템. 여기서 처음으로 "상호작용" 개념이 들어온다.

### 2026-08-05 · T-008 · 그리드 4방향 이동과 충돌
- 한 일: `core/world/movement.ts` (순수 이동 판정), OverworldScene 입력·트윈 연결, `sceneMarker.ts` → `domState.ts` 로 확장. 테스트 76 → 89개
- 알게 된 것:
  - **막혀 있어도 방향은 바뀐다.** 고전 JRPG 관습이자 실용적 이유가 있다 — 벽을 마주 본 채 조사하려면 제자리 회전이 필요하고, 막힌 방향에서 아무 반응이 없으면 입력이 씹힌 것처럼 느껴진다. 그래서 방향 표식(pip)을 붙여 회전이 눈에 보이게 했다.
  - **이동 중 입력을 큐에 쌓지 않고 버린다.** 누른 만큼 미끄러지면 조작감이 나빠진다.
  - `spawnWalker` 는 막힌 스폰 지점을 조용히 옆 칸으로 밀어내지 않고 던진다. 맵 데이터가 틀린 것이므로 고쳐야 한다.
  - **스모크에서 `keyboard.press()` 가 먹히지 않았다.** 게임이 매 프레임 눌림 상태를 폴링하는데(눌러서 계속 걷는 조작을 위해), 기본 press 는 즉시 떼기라 프레임 사이에 끼어 무시됐다. `delay: 60` 으로 몇 프레임 눌러둬야 한다. 이벤트 기반인 Enter 키가 잘 되던 것과 대비된다 — **입력 방식이 다르면 테스트 방식도 달라야 한다.**
- 다음에 걸릴 것 같은 것:
  - 이동 속도(110ms/칸)와 입력 잠금이 하드코딩돼 있다. 달리기나 속도 변경이 생기면 데이터로 빼야 한다.
  - 대각선 입력을 지원하지 않고 고정 순서로 하나만 고른다. 두 키 동시 입력 시 결과가 항상 같도록 한 선택인데, 조작감 평가는 실제 플레이 후에 해야 한다.
  - 다음은 T-009 카메라. 맵이 화면에 딱 맞는 전제가 여기서 깨진다.

### 2026-08-04 · T-007b · 실제 타일셋 렌더링
- 한 일: `.tmj` 의 `tilesets` 파싱, gid → 타일셋·프레임 매핑, RenderTexture 렌더러. 유적 맵이 Kenney Tiny Dungeon 시트로 그려진다. 테스트 67 → 76개
- 알게 된 것:
  - **맵의 `tilesets[].name` 을 파일 경로가 아니라 에셋 색인의 키로 쓴다.** Tiled 는 보통 이미지 상대 경로를 쓰는데, 그러면 색인을 우회하게 되고 에셋이 옮겨질 때 맵이 조용히 깨진다. 맵에는 이름만 두고 해석은 색인이 한다.
  - **모든 gid 가 어느 타일셋 범위에 드는지 검증한다.** 맵을 손으로 고치다 범위를 벗어나면 화면에 구멍이 뚫리는데, 그 원인을 런타임에서 역추적하는 건 루프가 가장 못하는 일이다.
  - 맵의 타일셋 키가 `assets/index.json` 에 실제로 있는지 확인하는 테스트를 붙여 **맵 ↔ 에셋 색인 사이의 연결을 닫았다.** 키 오타가 런타임까지 살아남지 못한다.
  - 충돌 레이어도 실제 gid 를 쓴다. 0/1 플래그로 두면 gid 범위 검증에서 예외를 만들어야 하는데, 예외 없는 규칙이 낫다. "0이 아니면 막힘"이라는 계약은 그대로다.
  - 플레이스홀더 경로는 gid 가 아니라 **충돌 여부**로 색을 정하도록 바꿨다. gid 의미는 타일셋마다 다르지만 "막혔는가"는 어느 맵에서나 같아서, 에셋이 없어도 형태를 알아볼 수 있다.
  - 타일 인덱스를 고르려고 시트를 7배 확대하고 번호를 얹어 렌더링했다(Playwright). 192x176 원본으로는 육안 식별이 불가능했다.
- 다음에 걸릴 것 같은 것:
  - 플레이어 표식이 모래 바닥과 색이 겹쳐 안 보였다. 대비색으로 바꿨지만, 스크린샷으로 진행을 확인하는 이상 **배경에 묻히는 표식은 없는 것과 같다**는 점을 T-008에서도 유의해야 한다.
  - 맵이 화면에 딱 맞는 전제는 여전하다. T-009 카메라가 이를 대체한다.

### 2026-08-04 · 벤더 팩 · 에셋 색인에 팩 개념 추가 (ADR-007)
- 한 일: `packs` 도입, Kenney Tiny Town + Tiny Dungeon 등재, 고아 검사·CREDITS 대조를 팩까지 확장. 테스트 57 → 67개
- 사고 기록:
  - **CI가 빨간 PR #4를 머지했다.** PowerShell에 `&&` 가 없어서 `gh pr checks` 다음 줄의 `gh pr merge` 가 실패와 무관하게 실행됐다. main이 깨졌고 PR #5로 복구했다. `next-task.md` 8단계에 `if ($?)` 게이트를 명문화했다(PR #6).
  - 실패의 **원인 자체는 가드레일이 제대로 작동한 결과**였다. `git add -A` 가 작업 트리에 있던 Kenney zip 두 개를 쓸어담았고, T-007a에서 만든 고아 검사가 정확히 잡았다. 신호는 옳았고 내가 무시했다.
- 알게 된 것:
  - **팩 안의 항목은 `source` 를 생략하고 팩의 출처를 물려받는다.** 중복 기재를 강제하면 팩과 항목이 어긋날 여지가 생긴다(팩은 CC0인데 항목은 CC-BY라고 적히는 식).
  - `assets` 전체를 팩으로 선언하는 것과 팩 간 중첩을 금지했다. 전자는 고아 검사를 무력화하고, 후자는 소유가 모호해진다. 이 검사는 형식 검사보다 **먼저** 한다 — 걸리는 이유가 "경로 형식"이 아니라 "규칙의 근간"이라 메시지가 달라야 하기 때문이다.
  - Tiled의 타일셋 정의 파일이 `.tsx` 확장자를 쓴다. ESLint가 이를 TypeScript JSX로 파싱하려다 깨졌다. `assets/` 를 린트 대상에서 제외했다 — 어차피 우리 코드가 아니다.
- 다음에 걸릴 것 같은 것:
  - 팩 내부 파일은 용도가 기록되지 않는다. 실제로 쓰는 파일을 `entries` 에 올리는 규율이 무너지면 "어떤 시트를 쓰고 있었지"를 알 수 없게 된다.
  - T-007b(gid → 타일셋 좌표 매핑)의 선행 조건이 풀렸다. 다음 차례다.

### 2026-08-04 · T-007 · 타일맵 로딩과 렌더링
- 한 일: `core/world/tilemap.ts` (Tiled .tmj 파서·검증·타일 질의), `game/world/paintTilemap.ts` (플레이스홀더 렌더러), `src/data/maps/ruin-entrance.tmj` (30x16), OverworldScene 연결. 테스트 38 → 57개
- 알게 된 것:
  - **Tiled 스펙 중 쓰는 것만 받고 나머지는 명시적으로 거부했다.** 무한 맵·등각 투영·오브젝트 레이어·압축 data 를 조용히 무시하면 "왜 안 그려지지"를 나중에 디버깅하게 되는데, 그건 루프가 가장 못하는 일이다. 압축 data 는 거부하면서 "Tiled에서 CSV로 내보내라"까지 알려준다.
  - **`isSolid()` 는 맵 밖을 막힌 것으로 본다.** 경계 검사를 호출부마다 반복하면 언젠가 하나를 빠뜨린다. T-008 이동 로직이 이 계약에 기댄다.
  - 실제 맵 파일에 대한 테스트를 따로 뒀다 — 외곽이 전부 막혀 있는지, 스폰 지점이 막히지 않았는지, **ground(보이는 것)와 collision(막힌 것)이 어긋나지 않는지**. 마지막 것이 콘텐츠 작업에서 가장 자주 나는 버그다.
  - 등록되지 않은 gid 는 건너뛰지 않고 마젠타로 칠한다. 조용한 구멍보다 눈에 띄는 오류가 낫다.
  - `.tmj` 는 `?raw` 로 불러와 반드시 `parseTiledMap` 을 통과시킨다. 확장자가 `.json` 이었다면 Vite 가 자동 파싱해서 검증을 건너뛸 수 있었다.
- 프로세스 변경:
  - 스크린샷 파일명에서 태스크 ID를 뺐다 (`M0-title.png` → `title.png`). 이터레이션마다 파일이 쌓이면 저장소가 부풀고 "지금 화면"이 어느 것인지 알 수 없게 된다. `next-task.md` 5단계도 함께 고쳤다.
- 다음에 걸릴 것 같은 것:
  - 맵이 화면에 딱 맞아서(480x256 vs 480x270) 카메라 없이도 보인다. T-009에서 맵이 커지면 이 전제가 깨진다. OverworldScene 의 중앙 정렬 코드는 그때 카메라로 대체된다.
  - 백로그 최상단이 "에셋 필요"(사람 몫)라 건너뛰었다. `next-task.md` 에 사람 몫 항목을 건너뛰는 규칙이 명시돼 있지 않다 — 다음 이터레이션 전에 보완이 필요하다.

### 2026-08-04 · T-007a · 에셋 색인 체계
- 한 일: `assets/index.json` 스키마와 검증(`src/core/assets/`), Phaser 로더 브릿지(`src/game/assets/queueAssets.ts`), 저장소 정합성 테스트, `assets/README.md`·`CREDITS.md`. 테스트 12 → 38개
- 알게 된 것:
  - **런타임 의존성을 늘리지 않으려고 Zod 대신 손으로 검증기를 썼다.** CLAUDE.md 규칙 8이 "런타임 의존성 추가 시 사용자 확인"을 요구한다. 색인 검증 하나 때문에 승인을 기다리며 멈추는 것보다, 좁은 검증기를 직접 쓰는 편이 낫다고 판단했다. Zod 도입은 스키마가 여러 개로 늘어나는 T-012에서 정식으로 제안한다.
  - 검증기는 **첫 문제에서 멈추지 않고 전부 모아서 보고**한다. 하나 고치고 재실행하는 왕복이 루프에게는 통째로 한 이터레이션이다.
  - 없는 키를 조회하면 던지되, **에러 메시지에 다음에 할 일을 적었다** ("플레이스홀더로 진행하고 백로그에 추가하라"). 이 메시지는 사람보다 루프가 더 자주 읽는다.
  - 참조 스캐너가 자기 자신의 주석과 안내 문구를 오탐했다. 보증을 약화시키는 대신 정밀도를 올리고(공백 불허, 색인 자신 예외), **스캐너가 실제로 잡는지 증명하는 자체 테스트**를 붙였다. 발동하지 않는 가드레일은 없는 것과 같다.
- 다음에 걸릴 것 같은 것:
  - 에셋이 하나도 없어서 정합성 테스트 대부분이 자명하게 통과한다. 첫 에셋이 들어올 때 규칙이 실제로 물리는지 확인해야 한다.
  - 백로그에 "에셋 필요" 항목을 사람 몫으로 올려뒀다. 없어도 T-007·T-008은 플레이스홀더로 진행 가능하다.

### 2026-08-04 · T-001~T-006 · M0 부트스트랩
- 한 일: Vite + TS(strict) + Phaser 3 프로젝트 구성, `npm run verify` 파이프라인, 결정론적 RNG(mulberry32) + 12 테스트, Playwright 스모크, GitHub Actions CI, Boot→Title→Overworld 씬 골격
- 알게 된 것:
  - ADR-001/002를 문서로만 두지 않고 **ESLint 규칙으로 강제**했다. `src/core/**`의 phaser·window·document import 금지, 전 소스의 `Math.random()` 금지. 자율 루프는 문서를 어길 수 있지만 린트는 못 어긴다.
  - 씬 전환 검증을 스크린샷 픽셀 비교가 아니라 `body[data-scene]` 속성 질의로 했다 (`src/game/sceneMarker.ts`). 취약하지 않고 루프가 읽기 쉽다. **씬을 추가하면 반드시 `markScene()`을 호출해야 한다.**
  - npm 11이 install script를 기본 차단해서 esbuild postinstall 경고가 뜨지만, 플랫폼별 optional dependency로 바이너리가 들어와 빌드에는 지장이 없었다.
- 다음에 걸릴 것 같은 것:
  - 번들이 1.48MB(gzip 341KB)다. Phaser 전체를 싣고 있어서인데, 지금은 문제가 아니지만 M6 폴리시에서 커스텀 빌드나 코드 스플리팅을 검토해야 한다.
  - `npm run sim`은 아직 없다 (M3에서 생성). CLAUDE.md의 명령어 표에 (M3) 표시를 달아뒀다.
  - T-007a 에셋 색인이 다음 차례다. 에셋 팩이 아직 없으므로 빈 색인 + 플레이스홀더 규칙만 세우고 넘어간다.

### 2026-08-04 · T-000 · 설계 및 저장소 초기화
- 한 일: 아키텍처/기획서/백로그/자동화 설계 문서 작성, Node.js 24 · GitHub CLI 설치, 저장소 초기화
- 알게 된 것: 백지 환경이라 스택 제약이 없었음. 자율 검증 가능성을 최우선 기준으로 TypeScript + Phaser 3 + Vite 선택
- 다음에 걸릴 것 같은 것: M0(T-001~T-006)로 검증 파이프라인을 세우기 전까지는 루프를 켜지 않는다
