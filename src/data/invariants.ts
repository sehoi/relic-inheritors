/**
 * GDD §5.5 밸런스 불변식의 수치 (ADR-005, CLAUDE.md 규칙 6).
 *
 * 기준값을 테스트 안에 적으면 "왜 25%p 인가" 를 물을 곳이 없어진다. 여기 모아두면
 * 기준을 바꾸는 일이 커밋 하나로 드러나고, 바꿨다는 사실 자체가 리뷰 대상이 된다.
 */

export const RELIC_INVARIANTS = {
  /**
   * 불변식 1 · 지배 전략 부재.
   *
   * 승률 1위와 이 순위 사이의 차이가 상한을 넘으면 "이 조합만 정답" 이라는 뜻이다.
   *
   * **크기가 같은 조합끼리만 비교한다** (ADR-013). 섞어서 재면 "많이 낄수록 이긴다" 가
   * 격차로 잡히는데, 그건 지배 전략이 아니라 슬롯이 남는다는 뜻이다.
   */
  dominanceRank: 5,
  maxRankSpread: 0.25,

  /**
   * 이 개수 미만인 크기 묶음은 건너뛴다.
   *
   * 조합이 하나뿐인 묶음에서는 격차가 무조건 0, 다양성이 무조건 0 이다.
   * 통과해도 실패해도 아무 뜻이 없으므로 재지 않는다 — 대신 **건너뛴 사실을 출력한다.**
   */
  minClassSize: 2,

  /**
   * 불변식 2 · 조합 다양성.
   *
   * 승률이 이 구간에 드는 조합이 충분히 많아야 한다. 전부 이기거나 전부 지면
   * 조합을 고민할 이유가 없다.
   */
  healthyBand: { low: 0.55, high: 0.85 },
  minBandShare: 0.4,

  /**
   * 불변식 4 · 사장된 유물 부재.
   *
   * **"좋은 조합" 안에서만 센다.** 전체 조합에서 세면 채택률은 열거 방식이 정하는 상수가
   * 되어 아무것도 말하지 못한다. 상위 비율을 좁게 잡을수록 분해능이 올라가지만 잡음도 는다 —
   * 표본이 적은 지금은 30% 로 잡았다.
   *
   * 이것만은 **크기를 섞어서 잰다.** 혼자서는 약해도 조합에서 빛나는 유물이 있고
   * (`잿불 고리` 가 그렇다 — 단독 48%, 조합에서는 좋은 조합의 절반에 낀다),
   * 크기별로 재면 그런 유물이 사장됐다고 잘못 판정된다.
   */
  goodBuildRatio: 0.3,
  minAdoption: 0.05,
} as const;

/**
 * 조합 훑기 측정 지점.
 *
 * **파티 Lv20 vs 잡몹 Lv36.** 레벨 차가 크게 벌어져 보이지만, 이 지점을 고른 이유는
 * 게임이 여기서 균형 잡혀 있다고 보아서가 아니라 **여기서만 승률이 갈리기 때문이다.**
 *
 * 실측(유물 3종, 조합 7개, 시드 60)에서 난이도별 승률 분포는 이랬다:
 *
 * ```
 * 적 +14   100 100 100  98  97  95  92   ← 전부 이긴다. 조합의 차이가 승률에 안 나타난다
 * 적 +16    92  92  85  82  70  63  50   ← 갈린다
 * 적 +19    40  32  27  20  15   5   3   ← 전부 진다
 * ```
 *
 * 전부 이기거나 전부 지는 구간에서는 불변식이 **측정 자체를 못 한다.**
 * 그래서 분해능이 있는 지점을 골라 고정했다.
 *
 * ⚠️ **한 레벨 차이로 그림이 바뀐다.** 적 +17 에서는 승률 분포가 크게 흔들린다.
 * 지금 수치는 "이 지점에서는 지배 전략이 없다" 까지만 말하며, 그 이상을 주장하지 않는다.
 * 난이도 곡선 자체가 가파른 것은 별개의 문제이고, 유물이 늘어나면 다시 재야 한다.
 *
 * ⚠️ **시드 수가 판정을 바꾼다.** 시드 40으로 재면 격차가 22%p 로 통과하고, 300으로 재면
 * 26.7%p 로 실패했다 — 표본이 모자라 통과하는 불변식은 초록일 때 더 위험하다.
 * `tools/sim.ts` 와 같은 300을 쓴다. 판정과 리포트가 다른 표본을 쓰면
 * "테스트는 통과하는데 리포트는 이상한" 상태가 된다.
 */
export const BUILD_SWEEP = {
  partyLevel: 20,
  enemyLevel: 36,
  opponent: 'mob',
  trials: 300,
  /** 조합이 이보다 많으면 고르게 솎아낸다. 유물 12종이 되면 실행 시간이 문제가 된다. */
  maxBuilds: 60,
} as const;
